{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Welcome to Verger \ud83c\udf33","text":"<p>Verger is a lightweight, framework-agnostic tool that provides an interactive way to experiment with your AI prompts, models, and tools directly within your existing codebase.</p> <p>By loading your resources directly from your Python modules, Verger ensures that your experimentation environment is identical to your development environment. This eliminates the need to maintain configurations in multiple places and avoids the error-prone process of copy-pasting code or API keys into external tools.</p> <p>Stop copy-pasting. Stop secret-sharing. Embrace your code as the Single Source of Truth.</p>"},{"location":"#experiment-where-you-develop","title":"\ud83d\udee0\ufe0f Experiment where you Develop","text":"<p>Verger eliminates the friction between your code and your experimentation environment by providing a Single Source of Truth for your AI logic:</p> <ul> <li>\ud83c\udf31 No Dual Configuration: Use your prompts, models, and tools exactly as they are defined in your project. No need to re-configure them for a playground.</li> <li>\ud83d\udd12 Secure &amp; Local: Verger reuses your local <code>.env</code> files and environment variables. Your API keys and secrets stay where they belong\u2014on your machine.</li> <li>\u2696\ufe0f Environment Consistency: External playgrounds often use different versions or descriptions for tools (like a web-search tool) than your actual code. Verger ensures you experiment with your exact definitions and local libraries, so if it works in the playground, it works in your application.</li> <li>\u2728 Seamless Integration: Automatically imports your definitions. Whether it's a simple string prompt or a complex agentic tool, Verger bridges them into an interactive interface.</li> <li>\ud83e\udeb6 Minimalist: A tiny footprint with minimal dependencies, focused entirely on streamlining your developer experience.</li> </ul>"},{"location":"#how-it-works","title":"\u2699\ufe0f How it Works","text":"<p>Verger dynamically imports your models, prompts, and tools directly from your Python modules. It provides a unified Interactive Playground where you can:</p> <ol> <li>Select any prompt or tool from your codebase.</li> <li>Pick the models you want to evaluate.</li> <li>Experiment in real-time and see immediate results side-by-side.</li> </ol>"},{"location":"#key-features-at-a-glance","title":"\ud83d\ude80 Key Features at a Glance","text":"Feature Description Direct Loading Interacts with your Python objects directly\u2014no proxies, no middle-men. Single Source of Truth Your code is the configuration. No more manual synchronization. Environment Parity Experiment and develop in the exact same execution environment. Framework Agnostic Supports LangChain, PydanticAI, and native functions out of the box. Interactive Interface A high-focus environment designed for rapid iteration. <p>Ready to start experimenting?</p> <p>Check out the Quickstart Guide to get up and running in minutes! \ud83d\ude80</p>"},{"location":"contributing/","title":"Contributing to Verger","text":"<p>Thank you for your interest in contributing to Verger! This guide will help you get started.</p>"},{"location":"contributing/#quick-start","title":"\ud83d\ude80 Quick Start","text":""},{"location":"contributing/#1-fork-and-clone","title":"1. Fork and Clone","text":"<pre><code>git clone https://github.com/YOUR_USERNAME/verger.git\ncd verger\n</code></pre>"},{"location":"contributing/#2-set-up-development-environment","title":"2. Set Up Development Environment","text":"<p>Option 1: Using Make (Recommended) <pre><code>make setup     # Installs dependencies + git hooks\n</code></pre></p> <p>Option 2: Using uv directly <pre><code>uv sync --group dev\nuv run pre-commit install\nuv run pre-commit install --hook-type commit-msg\n</code></pre></p> <p>Git hooks will run before every commit and check:</p> <ul> <li>\u2705 Code formatting (Ruff)</li> <li>\u2705 Linting issues (Ruff)</li> <li>\u2705 Code modernization (refurb)</li> <li>\u2705 Type checking (ty)</li> <li>\u2705 Spelling (codespell)</li> <li>\u2705 Documentation integrity (mkdocs)</li> <li>\u2705 General hygiene (whitespace, line endings, etc.)</li> <li>\u2705 Commit message format (Commitizen)</li> <li>\u2705 Security issues (Bandit &amp; Detect-secrets)</li> <li>\u2705 Tests (pytest - runs on every commit)</li> </ul>"},{"location":"contributing/#commit-message-format","title":"\ud83d\udcdd Commit Message Format","text":"<p>We use Conventional Commits for clear and structured commit history. You can use <code>make cz</code> to help you craft a compliant commit message.</p>"},{"location":"contributing/#format","title":"Format","text":"<pre><code>&lt;type&gt;(&lt;scope&gt;): &lt;description&gt;\n\n[optional body]\n\n[optional footer]\n</code></pre>"},{"location":"contributing/#types","title":"Types","text":"<ul> <li><code>feat</code>: New feature</li> <li><code>fix</code>: Bug fix</li> <li><code>docs</code>: Documentation changes</li> <li><code>style</code>: Code style changes (formatting, no logic change)</li> <li><code>refactor</code>: Code refactoring</li> <li><code>perf</code>: Performance improvements</li> <li><code>test</code>: Adding or updating tests</li> <li><code>chore</code>: Maintenance tasks (dependencies, build, etc.)</li> <li><code>ci</code>: CI/CD changes</li> <li><code>build</code>: Build system changes</li> </ul>"},{"location":"contributing/#scope-optional","title":"Scope (Optional)","text":"<p>Use scope to specify which part of the codebase is affected: - <code>core</code>, <code>cli</code>, <code>tui</code>, <code>config</code>, <code>models</code>, <code>prompts</code>, <code>tests</code>, <code>docs</code></p>"},{"location":"contributing/#examples","title":"Examples","text":"<pre><code># Good commits \u2705\ngit commit -m \"feat: add support for new model provider\"\ngit commit -m \"feat(cli): implement verbose logging flag\"\ngit commit -m \"fix: resolve config loader environment variable precedence\"\ngit commit -m \"fix(tui): handle window resize events correctly\"\ngit commit -m \"docs: update architecture overview\"\ngit commit -m \"refactor(core): simplify plugin registration\"\ngit commit -m \"test(core): add unit tests for prompt registry\"\ngit commit -m \"chore: upgrade textual to v8.2.6\"\n\n# Bad commits \u274c\ngit commit -m \"fixed stuff\"\ngit commit -m \"WIP\"\ngit commit -m \"Updated files\"\ngit commit -m \"Fix bug\"\n</code></pre>"},{"location":"contributing/#breaking-changes","title":"Breaking Changes","text":"<p>If your change breaks backward compatibility, add <code>BREAKING CHANGE:</code> in the footer:</p> <pre><code>git commit -m \"feat(core): redesign plugin interface\n\nBREAKING CHANGE: The plugin interface now requires an 'initialize' method.\"\n</code></pre>"},{"location":"contributing/#code-quality-standards","title":"\ud83d\udd0d Code Quality Standards","text":""},{"location":"contributing/#before-committing","title":"Before Committing","text":"<p>Run these commands to ensure your code meets our standards:</p> <pre><code># Format and fix auto-fixable issues\nmake format\n\n# Check for remaining issues\nmake lint\n\n# Run type checking\nmake type-check\n\n# Suggest code modernizations\nmake refurb\n\n# Run security checks\nmake secure\n\n# Run tests with strict coverage enforcement\nmake test-cov\n\n# Check docstring coverage (100% required)\nmake doc-check\n\n# Run strict documentation build\nmake docs-build\n\n# Find unused code\nmake dead-code\n\n# Check for dependency issues\nmake dep-check\n\n# Run all quality checks at once\nmake quality\n\n# Run all pre-commit hooks manually\nmake pre-commit\n</code></pre>"},{"location":"contributing/#pre-commit-hooks","title":"Pre-commit Hooks","text":"<p>The pre-commit hooks will automatically run when you commit. If they fail:</p> <ol> <li>Review the errors - The hooks will show what needs to be fixed</li> <li>Fix the issues - Most formatting issues are auto-fixed</li> <li>Stage the changes - <code>git add .</code></li> <li>Commit again - <code>git commit -m \"your message\"</code></li> </ol>"},{"location":"contributing/#bypassing-hooks-not-recommended","title":"Bypassing Hooks (Not Recommended)","text":"<p>Only in emergencies: <pre><code>git commit --no-verify -m \"emergency fix\"\n</code></pre></p> <p>\u26a0\ufe0f Warning: CI will still check your code, so bypassing hooks locally doesn't help!</p>"},{"location":"contributing/#testing","title":"\ud83e\uddea Testing","text":""},{"location":"contributing/#running-tests","title":"Running Tests","text":"<pre><code># Run tests quickly without coverage\nmake test\n\n# Run tests with strict coverage enforcement (min 48%)\nmake test-cov\n\n# Run specific test file\nuv run pytest tests/core/config/test_loader.py\n\n# Run specific test\nuv run pytest tests/core/config/test_loader.py::test_loader_env_override\n</code></pre>"},{"location":"contributing/#writing-tests","title":"Writing Tests","text":"<ul> <li>Place tests in the <code>tests/</code> directory mirroring the <code>src/</code> structure.</li> <li>Use descriptive test names: <code>test_should_load_config_from_env</code></li> <li>We use <code>pytest</code> and <code>pytest-asyncio</code> for testing.</li> </ul>"},{"location":"contributing/#security","title":"\ud83d\udd12 Security","text":"<ul> <li>Never commit secrets, API keys, or credentials</li> <li>Use environment variables or <code>.env</code> files (which are ignored) for sensitive data</li> <li>Run <code>make secure</code> to check for secrets and common security issues</li> <li>Report security vulnerabilities privately to the maintainers</li> </ul>"},{"location":"contributing/#pull-request-process","title":"\ud83d\udccb Pull Request Process","text":""},{"location":"contributing/#1-create-a-feature-branch","title":"1. Create a Feature Branch","text":"<pre><code>git checkout -b feat/your-feature-name\n# or\ngit checkout -b fix/your-bug-fix\n</code></pre>"},{"location":"contributing/#2-make-your-changes","title":"2. Make Your Changes","text":"<ul> <li>Write clean, readable code</li> <li>Follow existing code style</li> <li>Add tests for new features</li> <li>Update documentation if needed</li> </ul>"},{"location":"contributing/#3-commit-your-changes","title":"3. Commit Your Changes","text":"<pre><code>git add .\ngit commit -m \"feat: add amazing feature\"\n# or use commitizen\nmake cz\n</code></pre> <p>The pre-commit hooks will run automatically.</p>"},{"location":"contributing/#4-push-to-your-fork","title":"4. Push to Your Fork","text":"<pre><code>git push origin feat/your-feature-name\n</code></pre>"},{"location":"contributing/#5-open-a-pull-request","title":"5. Open a Pull Request","text":"<ul> <li>Use a clear, descriptive title following Conventional Commits format</li> <li>Describe what changes you made and why</li> <li>Reference any related issues</li> <li>Ensure all CI checks pass</li> </ul>"},{"location":"contributing/#pr-title-format","title":"PR Title Format","text":"<p>Your PR title must follow Conventional Commits:</p> <pre><code>feat: add local model adapter\nfix(cli): resolve configuration path error\ndocs: update setup guide for windows\n</code></pre>"},{"location":"contributing/#ci-checks","title":"CI Checks","text":"<p>Your PR must pass: - \u2705 Code formatting (Ruff) - \u2705 Linting (Ruff) - \u2705 Type checking (ty) - \u2705 Security checks (Bandit &amp; Detect-secrets) - \u2705 Tests (pytest) - \u2705 Conventional Commits validation</p>"},{"location":"contributing/#development-workflow","title":"\ud83d\udee0\ufe0f Development Workflow","text":""},{"location":"contributing/#daily-development","title":"Daily Development","text":"<pre><code># 1. Pull latest changes\ngit pull origin main\n\n# 2. Create feature branch\ngit checkout -b feat/my-feature\n\n# 3. Make changes and test\nmake format\nmake test\n\n# 4. Commit (hooks run automatically or use make cz)\nmake cz\n\n# 5. Push\ngit push origin feat/my-feature\n\n# 6. Open PR on GitHub\n</code></pre>"},{"location":"contributing/#keeping-your-fork-updated","title":"Keeping Your Fork Updated","text":"<pre><code># Add upstream remote (once)\ngit remote add upstream https://github.com/ZAGHLAOUI/verger.git\n\n# Update your fork\ngit fetch upstream\ngit checkout main\ngit merge upstream/main\ngit push origin main\n</code></pre>"},{"location":"contributing/#code-style-guidelines","title":"\ud83d\udcda Code Style Guidelines","text":""},{"location":"contributing/#python","title":"Python","text":"<ul> <li>Follow PEP 8 (enforced by Ruff)</li> <li>Use type hints for all functions</li> <li>Keep functions small and focused</li> <li>Use descriptive variable names</li> <li>Avoid comments unless necessary (code should be self-documenting)</li> </ul>"},{"location":"contributing/#documentation","title":"Documentation","text":"<ul> <li>Update <code>README.md</code> for user-facing changes</li> <li>Update files in <code>docs/</code> if the architecture or guides change</li> <li>Use Google-style docstrings for public APIs. 100% docstring coverage is mandatory (enforced by <code>interrogate</code>).</li> </ul>"},{"location":"contributing/#reporting-bugs","title":"\ud83d\udc1b Reporting Bugs","text":""},{"location":"contributing/#before-reporting","title":"Before Reporting","text":"<ol> <li>Check existing issues</li> <li>Try the latest version</li> <li>Reproduce the bug consistently</li> </ol>"},{"location":"contributing/#bug-report-template","title":"Bug Report Template","text":"<pre><code>**Describe the bug**\nA clear description of what the bug is.\n\n**To Reproduce**\nSteps to reproduce the behavior:\n1. ...\n2. ...\n\n**Expected behavior**\nWhat you expected to happen.\n\n**Environment**\n- OS: [e.g., Ubuntu 22.04]\n- Python version: [e.g., 3.12]\n- Verger version: [e.g., 0.1.0]\n\n**Additional context**\nAny other relevant information.\n</code></pre>"},{"location":"contributing/#feature-requests","title":"\ud83d\udca1 Feature Requests","text":"<p>We welcome feature requests! Please: 1. Check if it's already requested 2. Describe the use case 3. Explain why it would be valuable 4. Consider contributing the implementation</p>"},{"location":"contributing/#code-of-conduct","title":"\ud83e\udd1d Code of Conduct","text":"<ul> <li>Be respectful and inclusive</li> <li>Provide constructive feedback</li> <li>Focus on what is best for the community</li> <li>Show empathy towards others</li> </ul>"},{"location":"contributing/#recognition","title":"\ud83c\udf89 Recognition","text":"<p>Contributors will be: - Listed in our README - Mentioned in release notes - Part of our growing community</p> <p>Thank you for contributing to Verger! \ud83d\ude80</p>"},{"location":"quickstart/","title":"Quick Start","text":"<p>Get up and running with Verger in three simple steps.</p>"},{"location":"quickstart/#1-install-verger","title":"1. Install Verger","text":"<p>If you are using uv (recommended): <pre><code>uv add --dev verger\n</code></pre></p> <p>If you prefer pip: <pre><code>pip install verger\n</code></pre></p>"},{"location":"quickstart/#2-configure-your-project","title":"2. Configure your project","text":"<p>Create a <code>verger.toml</code> file in your project root. Verger is zero-invasive; it doesn't require any changes to your code. You simply tell it where your existing resources are and which <code>.env</code> file to use (it defaults to <code>.env</code>).</p> <p>Verger will automatically import your models, prompts, and tools from your modules.</p> <p>verger.toml <pre><code>env_file = \".env\"  # Optional, defaults to .env\n\n[prompts]\ntranslator = \"myapp.prompts:SYSTEM_MSG\"\n\n[models]\ngpt4 = \"myapp.agents:openai_model\"\n\n[tools]\nsearch_tool = \"myapp.tools:web_search\"\n</code></pre></p> <p>If you prefer to keep all your configuration in one place, you can use <code>pyproject.toml</code> instead by adding a <code>[tool.verger]</code> section:</p> <p>pyproject.toml <pre><code>[tool.verger]\nenv_file = \".env\"  # Optional, defaults to .env\n\n[tool.verger.prompts]\ntranslator = \"myapp.prompts:SYSTEM_MSG\"\n\n[tool.verger.models]\ngpt4 = \"myapp.agents:openai_model\"\n\n[tool.verger.tools]\nsearch_tool = \"myapp.tools:web_search\"\n</code></pre></p>"},{"location":"quickstart/#3-launch-the-tui","title":"3. Launch the TUI","text":"<p>Run Verger to interact with your prompts and test your models:</p> <pre><code>uv run verger tui\n</code></pre>"},{"location":"architecture/overview/","title":"Architecture Overview","text":"<p>Verger is built on three main layers to ensure it remains fast, extensible, and easy to maintain.</p>"},{"location":"architecture/overview/#1-the-core-the-brain","title":"1. The Core (The Brain)","text":"<p>The core handles the \"heavy lifting\" of the application. It is completely independent of the UI. - Registry Pattern: All model and prompt support is loaded dynamically. - Adapters: We wrap external libraries (like LangChain) in a unified <code>VergerModel</code> interface. - AST Loader: We use Python's <code>ast</code> module to surgically read and write prompts in your source code.</p>"},{"location":"architecture/overview/#2-the-cli-the-voice","title":"2. The CLI (The Voice)","text":"<p>Built with Typer, the CLI provides a fast way to interact with your prompts and models from the command line. - Commands: <code>ver prompts list</code>, <code>ver run</code>, etc.</p>"},{"location":"architecture/overview/#3-the-tui-the-face","title":"3. The TUI (The Face)","text":"<p>Built with Textual, the TUI provides a rich, interactive environment for evaluating AI responses side-by-side.</p>"},{"location":"architecture/overview/#data-flow","title":"Data Flow","text":"<ol> <li>Bootstrap: System loads <code>.env</code> and discovers plugins via Entry Points.</li> <li>Discovery: User code is scanned (via AST for prompts, via Import for models).</li> <li>Execution: The <code>Runner</code> executes prompts against models using async adapters.</li> </ol>"},{"location":"guide/cli/","title":"CLI Usage","text":"<p>Verger provides a set of commands to interact with your AI assets directly from the terminal.</p>"},{"location":"guide/cli/#commands","title":"Commands","text":""},{"location":"guide/cli/#verger-info","title":"<code>verger info</code>","text":"<p>Check if Verger is correctly installed and displays the count of found prompts and models in your project.</p>"},{"location":"guide/cli/#verger-list-prompts","title":"<code>verger list-prompts</code>","text":"<p>List all prompts defined in your configuration along with their references.</p>"},{"location":"guide/cli/#verger-tui","title":"<code>verger tui</code>","text":"<p>Launch the full interactive experience. The TUI allows you to: - View Prompts: See all your configured prompts in one place. - Live Preview: See the actual text of your prompts extracted via AST. - Side-by-Side Comparison: (Upcoming) Compare how different models respond to the same prompt.</p>"},{"location":"guide/cli/#global-options","title":"Global Options","text":""},{"location":"guide/cli/#verger_logdebug","title":"<code>VERGER_LOG=DEBUG</code>","text":"<p>Set this environment variable to see detailed logs about plugin discovery and configuration loading.</p> <pre><code>VERGER_LOG=DEBUG verger info\n</code></pre>"},{"location":"guide/config/","title":"Configuration Guide","text":"<p>Verger is designed to be \"Zero-Invasive.\" You don't need to change your code; you just tell Verger where to look in your configuration file.</p>"},{"location":"guide/config/#configuration-files","title":"Configuration Files","text":"<p>Verger looks for configuration in the following order:</p> <ol> <li><code>VERGER_CONFIG</code> environment variable: Path to a specific TOML file.</li> <li><code>verger.toml</code>: A standalone configuration file in your project root.</li> <li><code>pyproject.toml</code>: The <code>[tool.verger]</code> section in your project's standard configuration file.</li> </ol>"},{"location":"guide/config/#vergertoml-example","title":"<code>verger.toml</code> example","text":"<p>You can put your configuration in a dedicated file:</p> <pre><code>env_file = \".env\"\n\n[prompts]\nsystem_msg = \"src/agents/prompts:SYSTEM_PROMPT\"\n\n[models]\ngpt4_agent = { ref = \"src.agents.main:agent\" }\n</code></pre>"},{"location":"guide/config/#pyprojecttoml-example","title":"<code>pyproject.toml</code> example","text":"<p>Alternatively, add a <code>[tool.verger]</code> section to your <code>pyproject.toml</code>:</p> <pre><code>[tool.verger]\nenv_file = \".env\"\n\n[tool.verger.prompts]\nsystem_msg = \"src/agents/prompts:SYSTEM_PROMPT\"\n\n[tool.verger.models]\ngpt4_agent = { ref = \"src.agents.main:agent\" }\n</code></pre>"},{"location":"guide/config/#how-discovery-works","title":"How Discovery Works","text":""},{"location":"guide/config/#prompts","title":"Prompts","text":"<p>Verger uses AST (Static Analysis) to read prompts. This means: 1. It finds the file you pointed to. 2. It looks for the variable name. 3. It extracts the string value without running your code.</p>"},{"location":"guide/config/#models","title":"Models","text":"<p>Verger uses Dynamic Imports for models. 1. It imports the module and gets the object. 2. It passes the object through our Registry. 3. It automatically wraps it in the correct Adapter (e.g., LangChain Adapter).</p>"},{"location":"reference/core/","title":"Core Engine","text":"<p>This section documents the core execution logic and initialization systems of Verger.</p>"},{"location":"reference/core/#verger.core.engine","title":"<code>verger.core.engine</code>","text":"<p>The core execution engine for running prompts against models.</p>"},{"location":"reference/core/#verger.core.engine.ExecutionEngine","title":"<code>ExecutionEngine</code>","text":"<p>The orchestrator that brings together models, prompts, and variables. It resolves the requested resources and executes the logic.</p> Source code in <code>src/verger/core/engine.py</code> <pre><code>class ExecutionEngine:\n    \"\"\"\n    The orchestrator that brings together models, prompts, and variables.\n    It resolves the requested resources and executes the logic.\n    \"\"\"\n\n    async def run(\n        self,\n        model_obj: Any,\n        prompt_obj: Any,\n        variables: dict[str, Any] | None = None,\n        tools: list[Any] | None = None,\n        **kwargs: Any,\n    ) -&gt; VergerMessage:\n        \"\"\"\n        Loads the prompt, fills it with variables, and runs it on the model.\n\n        Args:\n            model_obj: The model instance (or object to be resolved)\n            prompt_obj: The prompt instance (or object to be resolved)\n            variables: A dictionary of variables to inject into the prompt\n            tools: A list of tool objects to be resolved and passed to the model\n            **kwargs: Additional parameters to pass to the model's invoke method\n\n        Returns:\n            The VergerMessage response from the model.\n        \"\"\"\n        # 1. Resolve the model adapter\n        model = model_registry.resolve(model_obj)\n\n        # 2. Resolve and load the prompt adapter\n        prompt = prompt_registry.resolve(prompt_obj)\n\n        # 3. Resolve tool adapters\n        resolved_tools = [tool_registry.resolve(t) for t in (tools or [])]\n\n        # 4. Format the prompt messages and invoke the model\n        formatted_messages = prompt.format(**(variables or {}))\n\n        return await model.invoke(\n            messages=formatted_messages,\n            tools=resolved_tools or None,\n            **kwargs,\n        )\n</code></pre>"},{"location":"reference/core/#verger.core.engine.ExecutionEngine.run","title":"<code>run(model_obj, prompt_obj, variables=None, tools=None, **kwargs)</code>  <code>async</code>","text":"<p>Loads the prompt, fills it with variables, and runs it on the model.</p> <p>Parameters:</p> Name Type Description Default <code>model_obj</code> <code>Any</code> <p>The model instance (or object to be resolved)</p> required <code>prompt_obj</code> <code>Any</code> <p>The prompt instance (or object to be resolved)</p> required <code>variables</code> <code>dict[str, Any] | None</code> <p>A dictionary of variables to inject into the prompt</p> <code>None</code> <code>tools</code> <code>list[Any] | None</code> <p>A list of tool objects to be resolved and passed to the model</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional parameters to pass to the model's invoke method</p> <code>{}</code> <p>Returns:</p> Type Description <code>VergerMessage</code> <p>The VergerMessage response from the model.</p> Source code in <code>src/verger/core/engine.py</code> <pre><code>async def run(\n    self,\n    model_obj: Any,\n    prompt_obj: Any,\n    variables: dict[str, Any] | None = None,\n    tools: list[Any] | None = None,\n    **kwargs: Any,\n) -&gt; VergerMessage:\n    \"\"\"\n    Loads the prompt, fills it with variables, and runs it on the model.\n\n    Args:\n        model_obj: The model instance (or object to be resolved)\n        prompt_obj: The prompt instance (or object to be resolved)\n        variables: A dictionary of variables to inject into the prompt\n        tools: A list of tool objects to be resolved and passed to the model\n        **kwargs: Additional parameters to pass to the model's invoke method\n\n    Returns:\n        The VergerMessage response from the model.\n    \"\"\"\n    # 1. Resolve the model adapter\n    model = model_registry.resolve(model_obj)\n\n    # 2. Resolve and load the prompt adapter\n    prompt = prompt_registry.resolve(prompt_obj)\n\n    # 3. Resolve tool adapters\n    resolved_tools = [tool_registry.resolve(t) for t in (tools or [])]\n\n    # 4. Format the prompt messages and invoke the model\n    formatted_messages = prompt.format(**(variables or {}))\n\n    return await model.invoke(\n        messages=formatted_messages,\n        tools=resolved_tools or None,\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/core/#verger.core.bootstrap","title":"<code>verger.core.bootstrap</code>","text":"<p>Initialization logic for setting up the Verger core.</p>"},{"location":"reference/core/#verger.core.bootstrap.setup_app","title":"<code>setup_app()</code>","text":"<p>Initializes the core logic by loading configuration, environment variables, and discovering plugins.</p> Source code in <code>src/verger/core/bootstrap.py</code> <pre><code>def setup_app():\n    \"\"\"\n    Initializes the core logic by loading configuration, environment variables,\n    and discovering plugins.\n    \"\"\"\n    # Inject Current Working Directory into sys.path so we can import user modules\n    cwd = str(Path.cwd())\n    if cwd not in sys.path:\n        sys.path.insert(0, cwd)\n\n    # Step 1: Load configuration\n    config = load_config()\n\n    # Step 2: Load environment variables (based on env_file in config)\n    env_keys = load_env()\n    # Store the keys that were actually loaded from the file\n    config.status.env_keys = env_keys\n\n    # Step 3: Discover plugins\n    load_plugins()\n</code></pre>"},{"location":"reference/core/#verger.core.plugins","title":"<code>verger.core.plugins</code>","text":"<p>Plugin discovery and dynamic loading system.</p>"},{"location":"reference/core/#verger.core.plugins.load_plugins","title":"<code>load_plugins()</code>","text":"<p>Entry point to load all internal adapters.</p> Source code in <code>src/verger/core/plugins.py</code> <pre><code>def load_plugins():\n    \"\"\"Entry point to load all internal adapters.\"\"\"\n    _register_model_adapters()\n    _register_prompt_adapters()\n    _register_tool_adapters()\n</code></pre>"},{"location":"reference/core/#verger.core.exceptions","title":"<code>verger.core.exceptions</code>","text":"<p>Custom exceptions for the Verger framework.</p>"},{"location":"reference/core/#verger.core.exceptions.VergerConfigurationError","title":"<code>VergerConfigurationError</code>","text":"<p>               Bases: <code>VergerError</code></p> <p>Raised when configuration loading fails or is invalid.</p> Source code in <code>src/verger/core/exceptions.py</code> <pre><code>class VergerConfigurationError(VergerError):\n    \"\"\"Raised when configuration loading fails or is invalid.\"\"\"\n\n    pass\n</code></pre>"},{"location":"reference/core/#verger.core.exceptions.VergerError","title":"<code>VergerError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Base exception for all Verger-related errors.</p> Source code in <code>src/verger/core/exceptions.py</code> <pre><code>class VergerError(Exception):\n    \"\"\"Base exception for all Verger-related errors.\"\"\"\n\n    pass\n</code></pre>"},{"location":"reference/core/#verger.core.exceptions.VergerResolutionError","title":"<code>VergerResolutionError</code>","text":"<p>               Bases: <code>VergerError</code></p> <p>Raised when an object (model, prompt, tool) cannot be resolved to a Verger adapter.</p> Source code in <code>src/verger/core/exceptions.py</code> <pre><code>class VergerResolutionError(VergerError):\n    \"\"\"Raised when an object (model, prompt, tool) cannot be resolved to a Verger adapter.\"\"\"\n\n    pass\n</code></pre>"},{"location":"reference/core/#verger.core.config.loader","title":"<code>verger.core.config.loader</code>","text":"<p>Configuration loading from verger.toml or pyproject.toml.</p>"},{"location":"reference/core/#verger.core.config.loader.get_config","title":"<code>get_config()</code>","text":"<p>Returns the loaded configuration. Raises error if not loaded.</p> Source code in <code>src/verger/core/config/loader.py</code> <pre><code>def get_config() -&gt; VergerConfig:\n    \"\"\"Returns the loaded configuration. Raises error if not loaded.\"\"\"\n    if _config is None:\n        raise RuntimeError(\"Verger configuration has not been loaded. Call load_config() first.\")\n    return _config\n</code></pre>"},{"location":"reference/core/#verger.core.config.loader.load_config","title":"<code>load_config(root_dir=None)</code>","text":"<p>Finds and parses the configuration file. Priority: 1. VERGER_CONFIG environment variable 2. verger.toml in CWD 3. pyproject.toml [tool.verger] in CWD</p> Source code in <code>src/verger/core/config/loader.py</code> <pre><code>def load_config(root_dir: Path | None = None) -&gt; VergerConfig:\n    \"\"\"\n    Finds and parses the configuration file.\n    Priority:\n    1. VERGER_CONFIG environment variable\n    2. verger.toml in CWD\n    3. pyproject.toml [tool.verger] in CWD\n    \"\"\"\n    global _config\n\n    # 1. Check for Environment Variable Override\n    if env_config_path := os.getenv(\"VERGER_CONFIG\"):\n        path = Path(env_config_path)\n        if path.exists():\n            _config = _load_from_file(path)\n            _config.status.config_source = str(path)\n            logger.debug(f\"Loaded configuration from environment override: {path}\")\n            return _config\n        else:\n            raise VergerConfigurationError(f\"VERGER_CONFIG points to non-existent file: {path}\")\n\n    root = root_dir or Path.cwd()\n\n    # 2. Check verger.toml\n    verger_toml_path = root / \"verger.toml\"\n    if verger_toml_path.exists():\n        _config = _load_from_file(verger_toml_path)\n        _config.status.config_source = str(verger_toml_path)\n        logger.debug(f\"Loaded configuration from {verger_toml_path}\")\n        return _config\n\n    # 3. Check pyproject.toml\n    pyproject_path = root / \"pyproject.toml\"\n    if pyproject_path.exists():\n        config_data = _parse_pyproject(pyproject_path)\n        if config_data:\n            _config = VergerConfig(**config_data)\n            _config.status.config_source = f\"{pyproject_path} ([tool.verger])\"\n            logger.debug(f\"Loaded configuration from {pyproject_path}\")\n            return _config\n\n    # Default: Empty config\n    logger.warning(\"No configuration file found. Using default settings.\")\n    _config = VergerConfig()\n    _config.status.config_source = \"Defaults (No file found)\"\n    return _config\n</code></pre>"},{"location":"reference/core/#verger.core.schema.config","title":"<code>verger.core.schema.config</code>","text":"<p>Pydantic schemas for Verger configuration.</p>"},{"location":"reference/core/#verger.core.schema.config.ModelConfig","title":"<code>ModelConfig</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Configuration for an AI model.</p> Source code in <code>src/verger/core/schema/config.py</code> <pre><code>class ModelConfig(BaseModel):\n    \"\"\"Configuration for an AI model.\"\"\"\n\n    ref: str\n    params: dict[str, Any] = Field(default_factory=dict)\n</code></pre>"},{"location":"reference/core/#verger.core.schema.config.RuntimeStatus","title":"<code>RuntimeStatus</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Internal state tracking for the Verger execution environment.</p> Source code in <code>src/verger/core/schema/config.py</code> <pre><code>class RuntimeStatus(BaseModel):\n    \"\"\"Internal state tracking for the Verger execution environment.\"\"\"\n\n    config_source: str = \"Unknown\"\n    env_keys: list[str] = Field(default_factory=list)\n</code></pre>"},{"location":"reference/core/#verger.core.schema.config.VergerConfig","title":"<code>VergerConfig</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>The main [tool.verger] configuration schema.</p> Source code in <code>src/verger/core/schema/config.py</code> <pre><code>class VergerConfig(BaseModel):\n    \"\"\"The main [tool.verger] configuration schema.\"\"\"\n\n    env_file: str | None = \".env\"\n\n    # Runtime status (not loaded from TOML)\n    status: RuntimeStatus = Field(default_factory=RuntimeStatus, exclude=True)\n\n    # Prompts are currently just a name mapped to a reference string\n    prompts: dict[str, str] = Field(default_factory=dict)\n\n    # Models can be a simple reference string or a detailed ModelConfig object\n    models: dict[str, str | ModelConfig] = Field(default_factory=dict)\n\n    # Tools are currently just a name mapped to a reference string\n    tools: dict[str, str] = Field(default_factory=dict)\n\n    def get_model_ref(self, model_name: str) -&gt; str:\n        \"\"\"Helper to get the string reference for a model regardless of its config style.\"\"\"\n        config = self.models.get(model_name)\n        if config is None:\n            raise KeyError(f\"Model '{model_name}' not found in configuration.\")\n        if isinstance(config, ModelConfig):\n            return config.ref\n        return config\n</code></pre>"},{"location":"reference/core/#verger.core.schema.config.VergerConfig.get_model_ref","title":"<code>get_model_ref(model_name)</code>","text":"<p>Helper to get the string reference for a model regardless of its config style.</p> Source code in <code>src/verger/core/schema/config.py</code> <pre><code>def get_model_ref(self, model_name: str) -&gt; str:\n    \"\"\"Helper to get the string reference for a model regardless of its config style.\"\"\"\n    config = self.models.get(model_name)\n    if config is None:\n        raise KeyError(f\"Model '{model_name}' not found in configuration.\")\n    if isinstance(config, ModelConfig):\n        return config.ref\n    return config\n</code></pre>"},{"location":"reference/core/#verger.core.config.env","title":"<code>verger.core.config.env</code>","text":"<p>Environment variable management and .env loading.</p>"},{"location":"reference/core/#verger.core.config.env.load_env","title":"<code>load_env(root_dir=None)</code>","text":"<p>Loads environment variables from the file specified in the configuration. Defaults to '.env' in the current working directory.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>A list of the keys that were loaded from the .env file.</p> Source code in <code>src/verger/core/config/env.py</code> <pre><code>def load_env(root_dir: Path | None = None) -&gt; list[str]:\n    \"\"\"\n    Loads environment variables from the file specified in the configuration.\n    Defaults to '.env' in the current working directory.\n\n    Returns:\n        A list of the keys that were loaded from the .env file.\n    \"\"\"\n    try:\n        config = get_config()\n        env_file_name = config.env_file or \".env\"\n\n        root = root_dir or Path.cwd()\n        env_path = root / env_file_name\n\n        if env_path.exists():\n            # Use load_dotenv for side-effects and dotenv_values to get the keys\n            load_dotenv(env_path)\n            logger.debug(f\"Loaded environment variables from {env_path}\")\n            return list(dotenv_values(env_path).keys())\n        else:\n            if config.env_file != \".env\":\n                # Only warn if the user explicitly specified a file that's missing\n                logger.warning(f\"Specified env_file not found: {env_path}\")\n\n    except Exception as e:\n        logger.error(f\"Failed to load environment variables: {e}\")\n\n    return []\n</code></pre>"},{"location":"reference/models/","title":"Models &amp; Adapters","text":"<p>This section documents the interfaces and adapters for AI models.</p>"},{"location":"reference/models/#base-interfaces","title":"Base Interfaces","text":""},{"location":"reference/models/#verger.core.models.model_base","title":"<code>verger.core.models.model_base</code>","text":"<p>Base interfaces for model adapters and resolvers.</p>"},{"location":"reference/models/#verger.core.models.model_base.ModelResolver","title":"<code>ModelResolver</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Interface for a Model Plugin.</p> <p>Resolvers are responsible for identifying objects from specific frameworks (e.g., LangChain) and wrapping them in a VergerModel adapter.</p> Source code in <code>src/verger/core/models/model_base.py</code> <pre><code>@runtime_checkable\nclass ModelResolver(Protocol):\n    \"\"\"\n    Interface for a Model Plugin.\n\n    Resolvers are responsible for identifying objects from specific frameworks\n    (e.g., LangChain) and wrapping them in a VergerModel adapter.\n    \"\"\"\n\n    priority: int = 50\n    \"\"\"Priority of the resolver (lower numbers are checked first).\"\"\"\n\n    def can_handle(self, obj: Any) -&gt; bool:\n        \"\"\"\n        Determine if this resolver can handle the given object.\n\n        Args:\n            obj: The model object to inspect.\n\n        Returns:\n            True if the resolver recognizes the object's framework.\n        \"\"\"\n        ...\n\n    def create_adapter(self, obj: Any) -&gt; VergerModel:\n        \"\"\"\n        Wrap the user's object in a VergerModel adapter.\n\n        Args:\n            obj: The model object to wrap.\n\n        Returns:\n            An instance of a VergerModel implementation.\n        \"\"\"\n        ...\n</code></pre>"},{"location":"reference/models/#verger.core.models.model_base.ModelResolver.priority","title":"<code>priority = 50</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Priority of the resolver (lower numbers are checked first).</p>"},{"location":"reference/models/#verger.core.models.model_base.ModelResolver.can_handle","title":"<code>can_handle(obj)</code>","text":"<p>Determine if this resolver can handle the given object.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The model object to inspect.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the resolver recognizes the object's framework.</p> Source code in <code>src/verger/core/models/model_base.py</code> <pre><code>def can_handle(self, obj: Any) -&gt; bool:\n    \"\"\"\n    Determine if this resolver can handle the given object.\n\n    Args:\n        obj: The model object to inspect.\n\n    Returns:\n        True if the resolver recognizes the object's framework.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/models/#verger.core.models.model_base.ModelResolver.create_adapter","title":"<code>create_adapter(obj)</code>","text":"<p>Wrap the user's object in a VergerModel adapter.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The model object to wrap.</p> required <p>Returns:</p> Type Description <code>VergerModel</code> <p>An instance of a VergerModel implementation.</p> Source code in <code>src/verger/core/models/model_base.py</code> <pre><code>def create_adapter(self, obj: Any) -&gt; VergerModel:\n    \"\"\"\n    Wrap the user's object in a VergerModel adapter.\n\n    Args:\n        obj: The model object to wrap.\n\n    Returns:\n        An instance of a VergerModel implementation.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/models/#verger.core.models.model_base.VergerModel","title":"<code>VergerModel</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>The universal interface that every Model Adapter must implement.</p> <p>The core execution logic of Verger only interacts with this interface, ensuring framework independence.</p> Source code in <code>src/verger/core/models/model_base.py</code> <pre><code>@runtime_checkable\nclass VergerModel(Protocol):\n    \"\"\"\n    The universal interface that every Model Adapter must implement.\n\n    The core execution logic of Verger only interacts with this interface,\n    ensuring framework independence.\n    \"\"\"\n\n    async def invoke(\n        self,\n        messages: list[VergerMessage],\n        tools: list[VergerTool] | None = None,\n        **kwargs: Any,\n    ) -&gt; VergerMessage:\n        \"\"\"\n        Run the messages through the model and return the AI's message.\n\n        Args:\n            messages: A list of formatted VergerMessage objects to send to the model.\n            tools: An optional list of VergerTool adapters available to the model.\n            **kwargs: Additional model-specific parameters (e.g., temperature).\n\n        Returns:\n            The VergerMessage response from the model (usually with role=AI).\n        \"\"\"\n        ...\n</code></pre>"},{"location":"reference/models/#verger.core.models.model_base.VergerModel.invoke","title":"<code>invoke(messages, tools=None, **kwargs)</code>  <code>async</code>","text":"<p>Run the messages through the model and return the AI's message.</p> <p>Parameters:</p> Name Type Description Default <code>messages</code> <code>list[VergerMessage]</code> <p>A list of formatted VergerMessage objects to send to the model.</p> required <code>tools</code> <code>list[VergerTool] | None</code> <p>An optional list of VergerTool adapters available to the model.</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional model-specific parameters (e.g., temperature).</p> <code>{}</code> <p>Returns:</p> Type Description <code>VergerMessage</code> <p>The VergerMessage response from the model (usually with role=AI).</p> Source code in <code>src/verger/core/models/model_base.py</code> <pre><code>async def invoke(\n    self,\n    messages: list[VergerMessage],\n    tools: list[VergerTool] | None = None,\n    **kwargs: Any,\n) -&gt; VergerMessage:\n    \"\"\"\n    Run the messages through the model and return the AI's message.\n\n    Args:\n        messages: A list of formatted VergerMessage objects to send to the model.\n        tools: An optional list of VergerTool adapters available to the model.\n        **kwargs: Additional model-specific parameters (e.g., temperature).\n\n    Returns:\n        The VergerMessage response from the model (usually with role=AI).\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/models/#registry","title":"Registry","text":""},{"location":"reference/models/#verger.core.models.model_registry","title":"<code>verger.core.models.model_registry</code>","text":"<p>Central registry for discovering and resolving AI model adapters.</p>"},{"location":"reference/models/#verger.core.models.model_registry.ModelRegistry","title":"<code>ModelRegistry</code>","text":"<p>Registry for Model Resolvers.</p> <p>This class serves as a central hub for all registered model adapters. It takes an unknown model object (e.g., from LangChain or a native function), identifies the appropriate resolver, and returns a unified VergerModel adapter.</p> Source code in <code>src/verger/core/models/model_registry.py</code> <pre><code>class ModelRegistry:\n    \"\"\"\n    Registry for Model Resolvers.\n\n    This class serves as a central hub for all registered model adapters. It takes an\n    unknown model object (e.g., from LangChain or a native function), identifies the\n    appropriate resolver, and returns a unified VergerModel adapter.\n    \"\"\"\n\n    def __init__(self):\n        self._resolvers: list[ModelResolver] = []\n\n    def register(self, resolver: ModelResolver) -&gt; None:\n        \"\"\"\n        Register a new model resolver.\n\n        The registry automatically sorts resolvers by priority (lower numbers first).\n\n        Args:\n            resolver: The ModelResolver instance to add to the registry.\n        \"\"\"\n        self._resolvers.append(resolver)\n        self._resolvers.sort(key=lambda r: getattr(r, \"priority\", 50))\n\n    def resolve(self, obj: Any) -&gt; VergerModel:\n        \"\"\"\n        Resolve an object into a unified VergerModel adapter.\n\n        This method iterates through all registered resolvers and returns the adapter\n        provided by the first resolver that can handle the given object.\n\n        Args:\n            obj: The object to be resolved (e.g., a model instance).\n\n        Returns:\n            The corresponding VergerModel adapter.\n\n        Raises:\n            VergerResolutionError: If no registered resolver can handle the provided object.\n        \"\"\"\n        for resolver in self._resolvers:\n            if resolver.can_handle(obj):\n                return resolver.create_adapter(obj)\n\n        raise VergerResolutionError(\n            f\"No model adapter found for object of type: {type(obj)}. \"\n            \"Please ensure the correct Verger model plugin is installed and registered.\"\n        )\n</code></pre>"},{"location":"reference/models/#verger.core.models.model_registry.ModelRegistry.register","title":"<code>register(resolver)</code>","text":"<p>Register a new model resolver.</p> <p>The registry automatically sorts resolvers by priority (lower numbers first).</p> <p>Parameters:</p> Name Type Description Default <code>resolver</code> <code>ModelResolver</code> <p>The ModelResolver instance to add to the registry.</p> required Source code in <code>src/verger/core/models/model_registry.py</code> <pre><code>def register(self, resolver: ModelResolver) -&gt; None:\n    \"\"\"\n    Register a new model resolver.\n\n    The registry automatically sorts resolvers by priority (lower numbers first).\n\n    Args:\n        resolver: The ModelResolver instance to add to the registry.\n    \"\"\"\n    self._resolvers.append(resolver)\n    self._resolvers.sort(key=lambda r: getattr(r, \"priority\", 50))\n</code></pre>"},{"location":"reference/models/#verger.core.models.model_registry.ModelRegistry.resolve","title":"<code>resolve(obj)</code>","text":"<p>Resolve an object into a unified VergerModel adapter.</p> <p>This method iterates through all registered resolvers and returns the adapter provided by the first resolver that can handle the given object.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The object to be resolved (e.g., a model instance).</p> required <p>Returns:</p> Type Description <code>VergerModel</code> <p>The corresponding VergerModel adapter.</p> <p>Raises:</p> Type Description <code>VergerResolutionError</code> <p>If no registered resolver can handle the provided object.</p> Source code in <code>src/verger/core/models/model_registry.py</code> <pre><code>def resolve(self, obj: Any) -&gt; VergerModel:\n    \"\"\"\n    Resolve an object into a unified VergerModel adapter.\n\n    This method iterates through all registered resolvers and returns the adapter\n    provided by the first resolver that can handle the given object.\n\n    Args:\n        obj: The object to be resolved (e.g., a model instance).\n\n    Returns:\n        The corresponding VergerModel adapter.\n\n    Raises:\n        VergerResolutionError: If no registered resolver can handle the provided object.\n    \"\"\"\n    for resolver in self._resolvers:\n        if resolver.can_handle(obj):\n            return resolver.create_adapter(obj)\n\n    raise VergerResolutionError(\n        f\"No model adapter found for object of type: {type(obj)}. \"\n        \"Please ensure the correct Verger model plugin is installed and registered.\"\n    )\n</code></pre>"},{"location":"reference/models/#built-in-adapters","title":"Built-in Adapters","text":""},{"location":"reference/models/#verger.core.models.adapters.langchain_adapter","title":"<code>verger.core.models.adapters.langchain_adapter</code>","text":"<p>Adapter for LangChain models and runnables.</p>"},{"location":"reference/models/#verger.core.models.adapters.langchain_adapter.LangChainAdapter","title":"<code>LangChainAdapter</code>","text":"<p>               Bases: <code>VergerModel</code></p> <p>Adapter for LangChain 'Runnable' objects.</p> <p>This adapter allows Verger to interact with any LangChain object that implements the Runnable interface (specifically the async <code>ainvoke</code> method).</p> Source code in <code>src/verger/core/models/adapters/langchain_adapter.py</code> <pre><code>class LangChainAdapter(VergerModel):\n    \"\"\"\n    Adapter for LangChain 'Runnable' objects.\n\n    This adapter allows Verger to interact with any LangChain object that implements\n    the Runnable interface (specifically the async `ainvoke` method).\n    \"\"\"\n\n    def __init__(self, runnable: Any):\n        \"\"\"\n        Initialize the LangChain adapter.\n\n        Args:\n            runnable: A LangChain object (e.g., ChatOpenAI, PromptTemplate, etc.).\n        \"\"\"\n        self.runnable = runnable\n\n    async def invoke(\n        self,\n        messages: list[VergerMessage],\n        tools: list[VergerTool] | None = None,\n        **kwargs: Any,\n    ) -&gt; VergerMessage:\n        \"\"\"\n        Invoke the LangChain runnable and return a VergerMessage result.\n\n        Args:\n            messages: A list of formatted messages.\n            tools: Optional list of tools to bind to the model.\n            **kwargs: Additional parameters to pass to ainvoke.\n\n        Returns:\n            A VergerMessage containing the AI's response.\n        \"\"\"\n        runnable = self.runnable\n\n        # Convert VergerMessage objects to LangChain tuple format\n        role_mapping = {\n            \"system\": \"system\",\n            \"user\": \"human\",\n            \"ai\": \"ai\",\n            \"tool\": \"tool\",\n        }\n        lc_messages = [(role_mapping.get(m.role, \"human\"), m.content) for m in messages]\n\n        # If tools are provided, try to bind them\n        if tools and hasattr(runnable, \"bind_tools\"):\n            runnable = runnable.bind_tools(tools)\n\n        result = await runnable.ainvoke(lc_messages, **kwargs)\n\n        # Extract content and return as VergerMessage\n        content = str(getattr(result, \"content\", result))\n        return VergerMessage(role=MessageRole.AI, content=content)\n</code></pre>"},{"location":"reference/models/#verger.core.models.adapters.langchain_adapter.LangChainAdapter.__init__","title":"<code>__init__(runnable)</code>","text":"<p>Initialize the LangChain adapter.</p> <p>Parameters:</p> Name Type Description Default <code>runnable</code> <code>Any</code> <p>A LangChain object (e.g., ChatOpenAI, PromptTemplate, etc.).</p> required Source code in <code>src/verger/core/models/adapters/langchain_adapter.py</code> <pre><code>def __init__(self, runnable: Any):\n    \"\"\"\n    Initialize the LangChain adapter.\n\n    Args:\n        runnable: A LangChain object (e.g., ChatOpenAI, PromptTemplate, etc.).\n    \"\"\"\n    self.runnable = runnable\n</code></pre>"},{"location":"reference/models/#verger.core.models.adapters.langchain_adapter.LangChainAdapter.invoke","title":"<code>invoke(messages, tools=None, **kwargs)</code>  <code>async</code>","text":"<p>Invoke the LangChain runnable and return a VergerMessage result.</p> <p>Parameters:</p> Name Type Description Default <code>messages</code> <code>list[VergerMessage]</code> <p>A list of formatted messages.</p> required <code>tools</code> <code>list[VergerTool] | None</code> <p>Optional list of tools to bind to the model.</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional parameters to pass to ainvoke.</p> <code>{}</code> <p>Returns:</p> Type Description <code>VergerMessage</code> <p>A VergerMessage containing the AI's response.</p> Source code in <code>src/verger/core/models/adapters/langchain_adapter.py</code> <pre><code>async def invoke(\n    self,\n    messages: list[VergerMessage],\n    tools: list[VergerTool] | None = None,\n    **kwargs: Any,\n) -&gt; VergerMessage:\n    \"\"\"\n    Invoke the LangChain runnable and return a VergerMessage result.\n\n    Args:\n        messages: A list of formatted messages.\n        tools: Optional list of tools to bind to the model.\n        **kwargs: Additional parameters to pass to ainvoke.\n\n    Returns:\n        A VergerMessage containing the AI's response.\n    \"\"\"\n    runnable = self.runnable\n\n    # Convert VergerMessage objects to LangChain tuple format\n    role_mapping = {\n        \"system\": \"system\",\n        \"user\": \"human\",\n        \"ai\": \"ai\",\n        \"tool\": \"tool\",\n    }\n    lc_messages = [(role_mapping.get(m.role, \"human\"), m.content) for m in messages]\n\n    # If tools are provided, try to bind them\n    if tools and hasattr(runnable, \"bind_tools\"):\n        runnable = runnable.bind_tools(tools)\n\n    result = await runnable.ainvoke(lc_messages, **kwargs)\n\n    # Extract content and return as VergerMessage\n    content = str(getattr(result, \"content\", result))\n    return VergerMessage(role=MessageRole.AI, content=content)\n</code></pre>"},{"location":"reference/models/#verger.core.models.adapters.langchain_adapter.LangChainResolver","title":"<code>LangChainResolver</code>","text":"<p>               Bases: <code>ModelResolver</code></p> <p>Resolver that detects LangChain objects using duck-typing.</p> <p>This resolver identifies LangChain objects without requiring 'langchain' to be installed as a mandatory dependency of Verger.</p> Source code in <code>src/verger/core/models/adapters/langchain_adapter.py</code> <pre><code>class LangChainResolver(ModelResolver):\n    \"\"\"\n    Resolver that detects LangChain objects using duck-typing.\n\n    This resolver identifies LangChain objects without requiring 'langchain'\n    to be installed as a mandatory dependency of Verger.\n    \"\"\"\n\n    priority: int = 20\n\n    def can_handle(self, obj: Any) -&gt; bool:\n        \"\"\"\n        Check if the object is a LangChain runnable.\n\n        Args:\n            obj: The object to inspect.\n\n        Returns:\n            True if the object has an 'ainvoke' method and belongs to a langchain module.\n        \"\"\"\n        # We don't import langchain here! We check structurally to avoid Dependency Errors.\n        # 1. Check if it has the standard LangChain method 'ainvoke'\n        # 2. Verify 'langchain' is somewhere in its module path.\n        if hasattr(obj, \"ainvoke\"):\n            # Check the class hierarchy (MRO) for langchain modules\n            for base in obj.__class__.__mro__:\n                if \"langchain\" in base.__module__:\n                    return True\n        return False\n\n    def create_adapter(self, obj: Any) -&gt; VergerModel:\n        \"\"\"\n        Create a LangChainAdapter for the given object.\n\n        Args:\n            obj: The LangChain runnable to wrap.\n\n        Returns:\n            A new LangChainAdapter instance.\n        \"\"\"\n        return LangChainAdapter(obj)\n</code></pre>"},{"location":"reference/models/#verger.core.models.adapters.langchain_adapter.LangChainResolver.can_handle","title":"<code>can_handle(obj)</code>","text":"<p>Check if the object is a LangChain runnable.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The object to inspect.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the object has an 'ainvoke' method and belongs to a langchain module.</p> Source code in <code>src/verger/core/models/adapters/langchain_adapter.py</code> <pre><code>def can_handle(self, obj: Any) -&gt; bool:\n    \"\"\"\n    Check if the object is a LangChain runnable.\n\n    Args:\n        obj: The object to inspect.\n\n    Returns:\n        True if the object has an 'ainvoke' method and belongs to a langchain module.\n    \"\"\"\n    # We don't import langchain here! We check structurally to avoid Dependency Errors.\n    # 1. Check if it has the standard LangChain method 'ainvoke'\n    # 2. Verify 'langchain' is somewhere in its module path.\n    if hasattr(obj, \"ainvoke\"):\n        # Check the class hierarchy (MRO) for langchain modules\n        for base in obj.__class__.__mro__:\n            if \"langchain\" in base.__module__:\n                return True\n    return False\n</code></pre>"},{"location":"reference/models/#verger.core.models.adapters.langchain_adapter.LangChainResolver.create_adapter","title":"<code>create_adapter(obj)</code>","text":"<p>Create a LangChainAdapter for the given object.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The LangChain runnable to wrap.</p> required <p>Returns:</p> Type Description <code>VergerModel</code> <p>A new LangChainAdapter instance.</p> Source code in <code>src/verger/core/models/adapters/langchain_adapter.py</code> <pre><code>def create_adapter(self, obj: Any) -&gt; VergerModel:\n    \"\"\"\n    Create a LangChainAdapter for the given object.\n\n    Args:\n        obj: The LangChain runnable to wrap.\n\n    Returns:\n        A new LangChainAdapter instance.\n    \"\"\"\n    return LangChainAdapter(obj)\n</code></pre>"},{"location":"reference/models/#verger.core.models.adapters.langchain_adapter.get_resolver","title":"<code>get_resolver()</code>","text":"<p>Entry point for the plugin system to get the LangChain resolver.</p> <p>Returns:</p> Type Description <code>ModelResolver</code> <p>An instance of LangChainResolver.</p> Source code in <code>src/verger/core/models/adapters/langchain_adapter.py</code> <pre><code>def get_resolver() -&gt; ModelResolver:\n    \"\"\"\n    Entry point for the plugin system to get the LangChain resolver.\n\n    Returns:\n        An instance of LangChainResolver.\n    \"\"\"\n    return LangChainResolver()\n</code></pre>"},{"location":"reference/models/#verger.core.models.adapters.native_adapter","title":"<code>verger.core.models.adapters.native_adapter</code>","text":"<p>Adapter for native Python functions used as models.</p>"},{"location":"reference/models/#verger.core.models.adapters.native_adapter.NativeFunctionAdapter","title":"<code>NativeFunctionAdapter</code>","text":"<p>               Bases: <code>VergerModel</code></p> <p>Adapter for a standard Python function used as an AI model.</p> <p>This allows any simple Python function (sync or async) to be treated as an AI model. It intelligently handles both string-based and message-based function signatures.</p> Source code in <code>src/verger/core/models/adapters/native_adapter.py</code> <pre><code>class NativeFunctionAdapter(VergerModel):\n    \"\"\"\n    Adapter for a standard Python function used as an AI model.\n\n    This allows any simple Python function (sync or async) to be treated as an\n    AI model. It intelligently handles both string-based and message-based\n    function signatures.\n    \"\"\"\n\n    def __init__(self, func: Callable[..., Any]):\n        \"\"\"\n        Initialize the native function adapter.\n\n        Args:\n            func: A callable to wrap as a model.\n        \"\"\"\n        self.func = func\n\n    async def invoke(\n        self,\n        messages: list[VergerMessage],\n        tools: list[VergerTool] | None = None,\n        **kwargs: Any,\n    ) -&gt; VergerMessage:\n        \"\"\"\n        Invoke the native function and return the result as a message.\n\n        Args:\n            messages: The list of formatted messages.\n            tools: Optional list of tools.\n            **kwargs: Additional parameters.\n\n        Returns:\n            A VergerMessage with role=AI containing the function's output.\n        \"\"\"\n        import inspect\n\n        sig = inspect.signature(self.func)\n        params = sig.parameters\n\n        # Prepare arguments based on what the function accepts\n        args_to_pass: dict[str, Any] = {}\n\n        if \"messages\" in params:\n            args_to_pass[\"messages\"] = messages\n        elif params:\n            # If it doesn't accept 'messages', join all content as a single 'prompt' string\n            # This provides backward compatibility for string-based functions.\n            full_prompt = \"\\n\\n\".join(m.content for m in messages)\n            # Find the first parameter name to pass the prompt to\n            first_param = list(params.keys())[0]\n            args_to_pass[first_param] = full_prompt\n\n        if \"tools\" in params:\n            args_to_pass[\"tools\"] = tools\n\n        # Execute\n        if asyncio.iscoroutinefunction(self.func):\n            result = await self.func(**args_to_pass, **kwargs)\n        else:\n            result = self.func(**args_to_pass, **kwargs)\n\n        return VergerMessage(role=MessageRole.AI, content=str(result))\n</code></pre>"},{"location":"reference/models/#verger.core.models.adapters.native_adapter.NativeFunctionAdapter.__init__","title":"<code>__init__(func)</code>","text":"<p>Initialize the native function adapter.</p> <p>Parameters:</p> Name Type Description Default <code>func</code> <code>Callable[..., Any]</code> <p>A callable to wrap as a model.</p> required Source code in <code>src/verger/core/models/adapters/native_adapter.py</code> <pre><code>def __init__(self, func: Callable[..., Any]):\n    \"\"\"\n    Initialize the native function adapter.\n\n    Args:\n        func: A callable to wrap as a model.\n    \"\"\"\n    self.func = func\n</code></pre>"},{"location":"reference/models/#verger.core.models.adapters.native_adapter.NativeFunctionAdapter.invoke","title":"<code>invoke(messages, tools=None, **kwargs)</code>  <code>async</code>","text":"<p>Invoke the native function and return the result as a message.</p> <p>Parameters:</p> Name Type Description Default <code>messages</code> <code>list[VergerMessage]</code> <p>The list of formatted messages.</p> required <code>tools</code> <code>list[VergerTool] | None</code> <p>Optional list of tools.</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional parameters.</p> <code>{}</code> <p>Returns:</p> Type Description <code>VergerMessage</code> <p>A VergerMessage with role=AI containing the function's output.</p> Source code in <code>src/verger/core/models/adapters/native_adapter.py</code> <pre><code>async def invoke(\n    self,\n    messages: list[VergerMessage],\n    tools: list[VergerTool] | None = None,\n    **kwargs: Any,\n) -&gt; VergerMessage:\n    \"\"\"\n    Invoke the native function and return the result as a message.\n\n    Args:\n        messages: The list of formatted messages.\n        tools: Optional list of tools.\n        **kwargs: Additional parameters.\n\n    Returns:\n        A VergerMessage with role=AI containing the function's output.\n    \"\"\"\n    import inspect\n\n    sig = inspect.signature(self.func)\n    params = sig.parameters\n\n    # Prepare arguments based on what the function accepts\n    args_to_pass: dict[str, Any] = {}\n\n    if \"messages\" in params:\n        args_to_pass[\"messages\"] = messages\n    elif params:\n        # If it doesn't accept 'messages', join all content as a single 'prompt' string\n        # This provides backward compatibility for string-based functions.\n        full_prompt = \"\\n\\n\".join(m.content for m in messages)\n        # Find the first parameter name to pass the prompt to\n        first_param = list(params.keys())[0]\n        args_to_pass[first_param] = full_prompt\n\n    if \"tools\" in params:\n        args_to_pass[\"tools\"] = tools\n\n    # Execute\n    if asyncio.iscoroutinefunction(self.func):\n        result = await self.func(**args_to_pass, **kwargs)\n    else:\n        result = self.func(**args_to_pass, **kwargs)\n\n    return VergerMessage(role=MessageRole.AI, content=str(result))\n</code></pre>"},{"location":"reference/models/#verger.core.models.adapters.native_adapter.NativeFunctionResolver","title":"<code>NativeFunctionResolver</code>","text":"<p>               Bases: <code>ModelResolver</code></p> <p>Resolver that detects if the object is a standard callable.</p> <p>This resolver is highly generic and should typically be registered as a fallback (last in the list).</p> Source code in <code>src/verger/core/models/adapters/native_adapter.py</code> <pre><code>class NativeFunctionResolver(ModelResolver):\n    \"\"\"\n    Resolver that detects if the object is a standard callable.\n\n    This resolver is highly generic and should typically be registered as a\n    fallback (last in the list).\n    \"\"\"\n\n    priority: int = 100\n\n    def can_handle(self, obj: Any) -&gt; bool:\n        \"\"\"\n        Check if the object is callable.\n\n        Args:\n            obj: The object to inspect.\n\n        Returns:\n            True if the object is a callable function or method.\n        \"\"\"\n        return callable(obj)\n\n    def create_adapter(self, obj: Any) -&gt; VergerModel:\n        \"\"\"\n        Create a NativeFunctionAdapter for the given callable.\n\n        Args:\n            obj: The callable to wrap.\n\n        Returns:\n            A new NativeFunctionAdapter instance.\n        \"\"\"\n        return NativeFunctionAdapter(obj)\n</code></pre>"},{"location":"reference/models/#verger.core.models.adapters.native_adapter.NativeFunctionResolver.can_handle","title":"<code>can_handle(obj)</code>","text":"<p>Check if the object is callable.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The object to inspect.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the object is a callable function or method.</p> Source code in <code>src/verger/core/models/adapters/native_adapter.py</code> <pre><code>def can_handle(self, obj: Any) -&gt; bool:\n    \"\"\"\n    Check if the object is callable.\n\n    Args:\n        obj: The object to inspect.\n\n    Returns:\n        True if the object is a callable function or method.\n    \"\"\"\n    return callable(obj)\n</code></pre>"},{"location":"reference/models/#verger.core.models.adapters.native_adapter.NativeFunctionResolver.create_adapter","title":"<code>create_adapter(obj)</code>","text":"<p>Create a NativeFunctionAdapter for the given callable.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The callable to wrap.</p> required <p>Returns:</p> Type Description <code>VergerModel</code> <p>A new NativeFunctionAdapter instance.</p> Source code in <code>src/verger/core/models/adapters/native_adapter.py</code> <pre><code>def create_adapter(self, obj: Any) -&gt; VergerModel:\n    \"\"\"\n    Create a NativeFunctionAdapter for the given callable.\n\n    Args:\n        obj: The callable to wrap.\n\n    Returns:\n        A new NativeFunctionAdapter instance.\n    \"\"\"\n    return NativeFunctionAdapter(obj)\n</code></pre>"},{"location":"reference/models/#verger.core.models.adapters.native_adapter.get_resolver","title":"<code>get_resolver()</code>","text":"<p>Entry point for the plugin system to get the Native function resolver.</p> <p>Returns:</p> Type Description <code>ModelResolver</code> <p>An instance of NativeFunctionResolver.</p> Source code in <code>src/verger/core/models/adapters/native_adapter.py</code> <pre><code>def get_resolver() -&gt; ModelResolver:\n    \"\"\"\n    Entry point for the plugin system to get the Native function resolver.\n\n    Returns:\n        An instance of NativeFunctionResolver.\n    \"\"\"\n    return NativeFunctionResolver()\n</code></pre>"},{"location":"reference/prompts/","title":"Prompts &amp; Adapters","text":"<p>This section documents the interfaces and adapters for AI prompts.</p>"},{"location":"reference/prompts/#base-interfaces","title":"Base Interfaces","text":""},{"location":"reference/prompts/#verger.core.prompts.prompt_base","title":"<code>verger.core.prompts.prompt_base</code>","text":"<p>Base interfaces for prompt adapters and resolvers.</p>"},{"location":"reference/prompts/#verger.core.prompts.prompt_base.PromptResolver","title":"<code>PromptResolver</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Interface for a Prompt Plugin.</p> <p>Resolvers are responsible for identifying objects from specific frameworks (e.g., standard strings) and wrapping them in a VergerPrompt adapter.</p> Source code in <code>src/verger/core/prompts/prompt_base.py</code> <pre><code>@runtime_checkable\nclass PromptResolver(Protocol):\n    \"\"\"\n    Interface for a Prompt Plugin.\n\n    Resolvers are responsible for identifying objects from specific frameworks\n    (e.g., standard strings) and wrapping them in a VergerPrompt adapter.\n    \"\"\"\n\n    priority: int = 50\n    \"\"\"Priority of the resolver (lower numbers are checked first).\"\"\"\n\n    def can_handle(self, obj: Any) -&gt; bool:\n        \"\"\"\n        Determine if this resolver can handle the given object.\n\n        Args:\n            obj: The prompt object to inspect.\n\n        Returns:\n            True if the resolver recognizes the object format.\n        \"\"\"\n        ...\n\n    def create_adapter(self, obj: Any) -&gt; VergerPrompt:\n        \"\"\"\n        Wrap the prompt object in a VergerPrompt adapter.\n\n        Args:\n            obj: The prompt object to wrap.\n\n        Returns:\n            An instance of a VergerPrompt implementation.\n        \"\"\"\n        ...\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.prompt_base.PromptResolver.priority","title":"<code>priority = 50</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Priority of the resolver (lower numbers are checked first).</p>"},{"location":"reference/prompts/#verger.core.prompts.prompt_base.PromptResolver.can_handle","title":"<code>can_handle(obj)</code>","text":"<p>Determine if this resolver can handle the given object.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The prompt object to inspect.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the resolver recognizes the object format.</p> Source code in <code>src/verger/core/prompts/prompt_base.py</code> <pre><code>def can_handle(self, obj: Any) -&gt; bool:\n    \"\"\"\n    Determine if this resolver can handle the given object.\n\n    Args:\n        obj: The prompt object to inspect.\n\n    Returns:\n        True if the resolver recognizes the object format.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.prompt_base.PromptResolver.create_adapter","title":"<code>create_adapter(obj)</code>","text":"<p>Wrap the prompt object in a VergerPrompt adapter.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The prompt object to wrap.</p> required <p>Returns:</p> Type Description <code>VergerPrompt</code> <p>An instance of a VergerPrompt implementation.</p> Source code in <code>src/verger/core/prompts/prompt_base.py</code> <pre><code>def create_adapter(self, obj: Any) -&gt; VergerPrompt:\n    \"\"\"\n    Wrap the prompt object in a VergerPrompt adapter.\n\n    Args:\n        obj: The prompt object to wrap.\n\n    Returns:\n        An instance of a VergerPrompt implementation.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.prompt_base.VergerPrompt","title":"<code>VergerPrompt</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>The interface for a loaded prompt.</p> <p>It provides the message templates and metadata about where it came from, regardless of the underlying prompt framework.</p> Source code in <code>src/verger/core/prompts/prompt_base.py</code> <pre><code>@runtime_checkable\nclass VergerPrompt(Protocol):\n    \"\"\"\n    The interface for a loaded prompt.\n\n    It provides the message templates and metadata about where it came from,\n    regardless of the underlying prompt framework.\n    \"\"\"\n\n    def get_messages(self) -&gt; list[VergerMessage]:\n        \"\"\"\n        Return the raw message templates of the prompt.\n\n        Returns:\n            A list of VergerMessage objects acting as templates.\n        \"\"\"\n        ...\n\n    def get_id(self) -&gt; str:\n        \"\"\"\n        Get a unique identifier for this prompt instance.\n\n        Returns:\n            A string identifier (e.g., class name or memory address).\n        \"\"\"\n        ...\n\n    def format(self, **kwargs: Any) -&gt; list[VergerMessage]:\n        \"\"\"\n        Fill the prompt templates with variables and return the resulting messages.\n\n        Args:\n            **kwargs: Variables to inject into the message templates.\n\n        Returns:\n            A list of fully formatted VergerMessage objects.\n        \"\"\"\n        ...\n\n    def get_variables(self) -&gt; set[str]:\n        \"\"\"\n        Extract the set of variable names required by this prompt.\n\n        This allows for inspection and validation of placeholders\n        before the prompt is formatted.\n\n        Returns:\n            A set of required variable names.\n        \"\"\"\n        ...\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.prompt_base.VergerPrompt.format","title":"<code>format(**kwargs)</code>","text":"<p>Fill the prompt templates with variables and return the resulting messages.</p> <p>Parameters:</p> Name Type Description Default <code>**kwargs</code> <code>Any</code> <p>Variables to inject into the message templates.</p> <code>{}</code> <p>Returns:</p> Type Description <code>list[VergerMessage]</code> <p>A list of fully formatted VergerMessage objects.</p> Source code in <code>src/verger/core/prompts/prompt_base.py</code> <pre><code>def format(self, **kwargs: Any) -&gt; list[VergerMessage]:\n    \"\"\"\n    Fill the prompt templates with variables and return the resulting messages.\n\n    Args:\n        **kwargs: Variables to inject into the message templates.\n\n    Returns:\n        A list of fully formatted VergerMessage objects.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.prompt_base.VergerPrompt.get_id","title":"<code>get_id()</code>","text":"<p>Get a unique identifier for this prompt instance.</p> <p>Returns:</p> Type Description <code>str</code> <p>A string identifier (e.g., class name or memory address).</p> Source code in <code>src/verger/core/prompts/prompt_base.py</code> <pre><code>def get_id(self) -&gt; str:\n    \"\"\"\n    Get a unique identifier for this prompt instance.\n\n    Returns:\n        A string identifier (e.g., class name or memory address).\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.prompt_base.VergerPrompt.get_messages","title":"<code>get_messages()</code>","text":"<p>Return the raw message templates of the prompt.</p> <p>Returns:</p> Type Description <code>list[VergerMessage]</code> <p>A list of VergerMessage objects acting as templates.</p> Source code in <code>src/verger/core/prompts/prompt_base.py</code> <pre><code>def get_messages(self) -&gt; list[VergerMessage]:\n    \"\"\"\n    Return the raw message templates of the prompt.\n\n    Returns:\n        A list of VergerMessage objects acting as templates.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.prompt_base.VergerPrompt.get_variables","title":"<code>get_variables()</code>","text":"<p>Extract the set of variable names required by this prompt.</p> <p>This allows for inspection and validation of placeholders before the prompt is formatted.</p> <p>Returns:</p> Type Description <code>set[str]</code> <p>A set of required variable names.</p> Source code in <code>src/verger/core/prompts/prompt_base.py</code> <pre><code>def get_variables(self) -&gt; set[str]:\n    \"\"\"\n    Extract the set of variable names required by this prompt.\n\n    This allows for inspection and validation of placeholders\n    before the prompt is formatted.\n\n    Returns:\n        A set of required variable names.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/prompts/#registry","title":"Registry","text":""},{"location":"reference/prompts/#verger.core.prompts.prompt_registry","title":"<code>verger.core.prompts.prompt_registry</code>","text":"<p>Central registry for discovering and resolving prompt adapters.</p>"},{"location":"reference/prompts/#verger.core.prompts.prompt_registry.PromptRegistry","title":"<code>PromptRegistry</code>","text":"<p>Registry for Prompt Resolvers.</p> <p>This class serves as a central hub for all registered prompt adapters. It takes an unknown prompt object (e.g., a string or a complex template), identifies the appropriate resolver, and returns a unified VergerPrompt adapter.</p> Source code in <code>src/verger/core/prompts/prompt_registry.py</code> <pre><code>class PromptRegistry:\n    \"\"\"\n    Registry for Prompt Resolvers.\n\n    This class serves as a central hub for all registered prompt adapters. It takes an\n    unknown prompt object (e.g., a string or a complex template), identifies the\n    appropriate resolver, and returns a unified VergerPrompt adapter.\n    \"\"\"\n\n    def __init__(self):\n        self._resolvers: list[PromptResolver] = []\n\n    def register(self, resolver: PromptResolver) -&gt; None:\n        \"\"\"\n        Register a new prompt resolver.\n\n        The registry automatically sorts resolvers by priority (lower numbers first).\n\n        Args:\n            resolver: The PromptResolver instance to add to the registry.\n        \"\"\"\n        self._resolvers.append(resolver)\n        self._resolvers.sort(key=lambda r: getattr(r, \"priority\", 50))\n\n    def resolve(self, obj: Any) -&gt; VergerPrompt:\n        \"\"\"\n        Resolve an object into a unified VergerPrompt adapter.\n\n        This method iterates through all registered resolvers and returns the adapter\n        provided by the first resolver that can handle the given object.\n\n        Args:\n            obj: The object to be resolved (e.g., a prompt instance).\n\n        Returns:\n            The corresponding VergerPrompt adapter.\n\n        Raises:\n            VergerResolutionError: If no registered resolver can handle the provided object.\n        \"\"\"\n        for resolver in self._resolvers:\n            if resolver.can_handle(obj):\n                return resolver.create_adapter(obj)\n\n        raise VergerResolutionError(\n            f\"No prompt adapter found for object of type: {type(obj)}. \"\n            \"Please ensure the correct Verger prompt plugin is installed and registered.\"\n        )\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.prompt_registry.PromptRegistry.register","title":"<code>register(resolver)</code>","text":"<p>Register a new prompt resolver.</p> <p>The registry automatically sorts resolvers by priority (lower numbers first).</p> <p>Parameters:</p> Name Type Description Default <code>resolver</code> <code>PromptResolver</code> <p>The PromptResolver instance to add to the registry.</p> required Source code in <code>src/verger/core/prompts/prompt_registry.py</code> <pre><code>def register(self, resolver: PromptResolver) -&gt; None:\n    \"\"\"\n    Register a new prompt resolver.\n\n    The registry automatically sorts resolvers by priority (lower numbers first).\n\n    Args:\n        resolver: The PromptResolver instance to add to the registry.\n    \"\"\"\n    self._resolvers.append(resolver)\n    self._resolvers.sort(key=lambda r: getattr(r, \"priority\", 50))\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.prompt_registry.PromptRegistry.resolve","title":"<code>resolve(obj)</code>","text":"<p>Resolve an object into a unified VergerPrompt adapter.</p> <p>This method iterates through all registered resolvers and returns the adapter provided by the first resolver that can handle the given object.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The object to be resolved (e.g., a prompt instance).</p> required <p>Returns:</p> Type Description <code>VergerPrompt</code> <p>The corresponding VergerPrompt adapter.</p> <p>Raises:</p> Type Description <code>VergerResolutionError</code> <p>If no registered resolver can handle the provided object.</p> Source code in <code>src/verger/core/prompts/prompt_registry.py</code> <pre><code>def resolve(self, obj: Any) -&gt; VergerPrompt:\n    \"\"\"\n    Resolve an object into a unified VergerPrompt adapter.\n\n    This method iterates through all registered resolvers and returns the adapter\n    provided by the first resolver that can handle the given object.\n\n    Args:\n        obj: The object to be resolved (e.g., a prompt instance).\n\n    Returns:\n        The corresponding VergerPrompt adapter.\n\n    Raises:\n        VergerResolutionError: If no registered resolver can handle the provided object.\n    \"\"\"\n    for resolver in self._resolvers:\n        if resolver.can_handle(obj):\n            return resolver.create_adapter(obj)\n\n    raise VergerResolutionError(\n        f\"No prompt adapter found for object of type: {type(obj)}. \"\n        \"Please ensure the correct Verger prompt plugin is installed and registered.\"\n    )\n</code></pre>"},{"location":"reference/prompts/#built-in-adapters","title":"Built-in Adapters","text":""},{"location":"reference/prompts/#verger.core.prompts.adapters.string_adapter","title":"<code>verger.core.prompts.adapters.string_adapter</code>","text":"<p>Adapter for native Python strings used as prompts.</p>"},{"location":"reference/prompts/#verger.core.prompts.adapters.string_adapter.NativeStringPrompt","title":"<code>NativeStringPrompt</code>","text":"<p>               Bases: <code>VergerPrompt</code></p> <p>A prompt defined as a raw string.</p> <p>This adapter handles standard Python strings, supporting f-string style placeholders (e.g., '{name}'). It automatically wraps the string into a single USER message.</p> Source code in <code>src/verger/core/prompts/adapters/string_adapter.py</code> <pre><code>class NativeStringPrompt(VergerPrompt):\n    \"\"\"\n    A prompt defined as a raw string.\n\n    This adapter handles standard Python strings, supporting f-string style\n    placeholders (e.g., '{name}'). It automatically wraps the string into\n    a single USER message.\n    \"\"\"\n\n    def __init__(self, text: str):\n        \"\"\"\n        Initialize the native string prompt.\n\n        Args:\n            text: The raw prompt template string.\n        \"\"\"\n        self.text = text\n\n    def get_messages(self) -&gt; list[VergerMessage]:\n        \"\"\"\n        Get the prompt as a list of message templates.\n\n        Returns:\n            A list containing a single USER message template.\n        \"\"\"\n        return [VergerMessage(role=MessageRole.USER, content=self.text)]\n\n    def get_id(self) -&gt; str:\n        \"\"\"\n        Get a unique identifier for this prompt instance.\n\n        Returns:\n            A string ID.\n        \"\"\"\n        return f\"StringPrompt-{id(self)}\"\n\n    def format(self, **kwargs: Any) -&gt; list[VergerMessage]:\n        \"\"\"\n        Format the prompt string and return it as a list of messages.\n\n        Args:\n            **kwargs: Variables to inject into the placeholders.\n\n        Returns:\n            A list containing one fully formatted USER message.\n        \"\"\"\n        formatted_content = self.text.format(**kwargs)\n        return [VergerMessage(role=MessageRole.USER, content=formatted_content)]\n\n    def get_variables(self) -&gt; set[str]:\n        \"\"\"\n        Identify all variable placeholders in the prompt text.\n\n        Parses the prompt text using `string.Formatter` to identify all named fields\n        found in the template (e.g., '{name}').\n\n        Returns:\n            A set of variable names.\n        \"\"\"\n        formatter = string.Formatter()\n        return {\n            field_name\n            for _, field_name, _, _ in formatter.parse(self.text)\n            if field_name is not None\n        }\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.adapters.string_adapter.NativeStringPrompt.__init__","title":"<code>__init__(text)</code>","text":"<p>Initialize the native string prompt.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>The raw prompt template string.</p> required Source code in <code>src/verger/core/prompts/adapters/string_adapter.py</code> <pre><code>def __init__(self, text: str):\n    \"\"\"\n    Initialize the native string prompt.\n\n    Args:\n        text: The raw prompt template string.\n    \"\"\"\n    self.text = text\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.adapters.string_adapter.NativeStringPrompt.format","title":"<code>format(**kwargs)</code>","text":"<p>Format the prompt string and return it as a list of messages.</p> <p>Parameters:</p> Name Type Description Default <code>**kwargs</code> <code>Any</code> <p>Variables to inject into the placeholders.</p> <code>{}</code> <p>Returns:</p> Type Description <code>list[VergerMessage]</code> <p>A list containing one fully formatted USER message.</p> Source code in <code>src/verger/core/prompts/adapters/string_adapter.py</code> <pre><code>def format(self, **kwargs: Any) -&gt; list[VergerMessage]:\n    \"\"\"\n    Format the prompt string and return it as a list of messages.\n\n    Args:\n        **kwargs: Variables to inject into the placeholders.\n\n    Returns:\n        A list containing one fully formatted USER message.\n    \"\"\"\n    formatted_content = self.text.format(**kwargs)\n    return [VergerMessage(role=MessageRole.USER, content=formatted_content)]\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.adapters.string_adapter.NativeStringPrompt.get_id","title":"<code>get_id()</code>","text":"<p>Get a unique identifier for this prompt instance.</p> <p>Returns:</p> Type Description <code>str</code> <p>A string ID.</p> Source code in <code>src/verger/core/prompts/adapters/string_adapter.py</code> <pre><code>def get_id(self) -&gt; str:\n    \"\"\"\n    Get a unique identifier for this prompt instance.\n\n    Returns:\n        A string ID.\n    \"\"\"\n    return f\"StringPrompt-{id(self)}\"\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.adapters.string_adapter.NativeStringPrompt.get_messages","title":"<code>get_messages()</code>","text":"<p>Get the prompt as a list of message templates.</p> <p>Returns:</p> Type Description <code>list[VergerMessage]</code> <p>A list containing a single USER message template.</p> Source code in <code>src/verger/core/prompts/adapters/string_adapter.py</code> <pre><code>def get_messages(self) -&gt; list[VergerMessage]:\n    \"\"\"\n    Get the prompt as a list of message templates.\n\n    Returns:\n        A list containing a single USER message template.\n    \"\"\"\n    return [VergerMessage(role=MessageRole.USER, content=self.text)]\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.adapters.string_adapter.NativeStringPrompt.get_variables","title":"<code>get_variables()</code>","text":"<p>Identify all variable placeholders in the prompt text.</p> <p>Parses the prompt text using <code>string.Formatter</code> to identify all named fields found in the template (e.g., '{name}').</p> <p>Returns:</p> Type Description <code>set[str]</code> <p>A set of variable names.</p> Source code in <code>src/verger/core/prompts/adapters/string_adapter.py</code> <pre><code>def get_variables(self) -&gt; set[str]:\n    \"\"\"\n    Identify all variable placeholders in the prompt text.\n\n    Parses the prompt text using `string.Formatter` to identify all named fields\n    found in the template (e.g., '{name}').\n\n    Returns:\n        A set of variable names.\n    \"\"\"\n    formatter = string.Formatter()\n    return {\n        field_name\n        for _, field_name, _, _ in formatter.parse(self.text)\n        if field_name is not None\n    }\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.adapters.string_adapter.NativeStringResolver","title":"<code>NativeStringResolver</code>","text":"<p>               Bases: <code>PromptResolver</code></p> <p>Adapter that recognizes native Python strings and wraps them.</p> Source code in <code>src/verger/core/prompts/adapters/string_adapter.py</code> <pre><code>class NativeStringResolver(PromptResolver):\n    \"\"\"\n    Adapter that recognizes native Python strings and wraps them.\n    \"\"\"\n\n    priority: int = 100\n\n    def can_handle(self, obj: Any) -&gt; bool:\n        \"\"\"\n        Check if the object is a string.\n\n        Args:\n            obj: The object to inspect.\n\n        Returns:\n            True if the object is a string instance.\n        \"\"\"\n        return isinstance(obj, str)\n\n    def create_adapter(self, obj: Any) -&gt; VergerPrompt:\n        \"\"\"\n        Create a NativeStringPrompt for the given string.\n\n        Args:\n            obj: The string to wrap.\n\n        Returns:\n            A new NativeStringPrompt instance.\n        \"\"\"\n        return NativeStringPrompt(obj)\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.adapters.string_adapter.NativeStringResolver.can_handle","title":"<code>can_handle(obj)</code>","text":"<p>Check if the object is a string.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The object to inspect.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the object is a string instance.</p> Source code in <code>src/verger/core/prompts/adapters/string_adapter.py</code> <pre><code>def can_handle(self, obj: Any) -&gt; bool:\n    \"\"\"\n    Check if the object is a string.\n\n    Args:\n        obj: The object to inspect.\n\n    Returns:\n        True if the object is a string instance.\n    \"\"\"\n    return isinstance(obj, str)\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.adapters.string_adapter.NativeStringResolver.create_adapter","title":"<code>create_adapter(obj)</code>","text":"<p>Create a NativeStringPrompt for the given string.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The string to wrap.</p> required <p>Returns:</p> Type Description <code>VergerPrompt</code> <p>A new NativeStringPrompt instance.</p> Source code in <code>src/verger/core/prompts/adapters/string_adapter.py</code> <pre><code>def create_adapter(self, obj: Any) -&gt; VergerPrompt:\n    \"\"\"\n    Create a NativeStringPrompt for the given string.\n\n    Args:\n        obj: The string to wrap.\n\n    Returns:\n        A new NativeStringPrompt instance.\n    \"\"\"\n    return NativeStringPrompt(obj)\n</code></pre>"},{"location":"reference/prompts/#verger.core.prompts.adapters.string_adapter.get_resolver","title":"<code>get_resolver()</code>","text":"<p>Entry point for the plugin system to get the Native string resolver.</p> <p>Returns:</p> Type Description <code>PromptResolver</code> <p>An instance of NativeStringResolver.</p> Source code in <code>src/verger/core/prompts/adapters/string_adapter.py</code> <pre><code>def get_resolver() -&gt; PromptResolver:\n    \"\"\"\n    Entry point for the plugin system to get the Native string resolver.\n\n    Returns:\n        An instance of NativeStringResolver.\n    \"\"\"\n    return NativeStringResolver()\n</code></pre>"},{"location":"reference/utils/","title":"Utilities &amp; CLI","text":"<p>This section documents internal utilities and the CLI entry points.</p>"},{"location":"reference/utils/#cli-tui-applications","title":"CLI &amp; TUI Applications","text":""},{"location":"reference/utils/#verger.cli.cli_app","title":"<code>verger.cli.cli_app</code>","text":"<p>Typer application defining the CLI commands for Verger.</p>"},{"location":"reference/utils/#verger.cli.cli_app.info","title":"<code>info()</code>","text":"<p>Display information about Verger and the current configuration.</p> Source code in <code>src/verger/cli/cli_app.py</code> <pre><code>@app.command()\ndef info():\n    \"\"\"Display information about Verger and the current configuration.\"\"\"\n    try:\n        config = get_config()\n        typer.secho(\"Verger Status: Ready\", fg=typer.colors.GREEN, bold=True)\n        typer.echo(f\"Prompts found: {len(config.prompts)}\")\n        typer.echo(f\"Models found: {len(config.models)}\")\n\n        if config.prompts:\n            typer.echo(\"\\nPrompts:\")\n            for name in config.prompts:\n                typer.echo(f\"  - {name}\")\n\n        if config.models:\n            typer.echo(\"\\nModels:\")\n            for name in config.models:\n                typer.echo(f\"  - {name}\")\n\n    except Exception as e:\n        typer.secho(f\"Error loading config: {e}\", fg=typer.colors.RED)\n</code></pre>"},{"location":"reference/utils/#verger.cli.cli_app.list_prompts","title":"<code>list_prompts()</code>","text":"<p>List all configured prompts.</p> Source code in <code>src/verger/cli/cli_app.py</code> <pre><code>@app.command()\ndef list_prompts():\n    \"\"\"List all configured prompts.\"\"\"\n    config = get_config()\n    if not config.prompts:\n        typer.echo(\"No prompts configured.\")\n        return\n\n    for name, ref in config.prompts.items():\n        typer.echo(f\"{name}: {ref}\")\n</code></pre>"},{"location":"reference/utils/#verger.cli.cli_app.tui","title":"<code>tui()</code>","text":"<p>Launch the Verger Terminal User Interface.</p> Source code in <code>src/verger/cli/cli_app.py</code> <pre><code>@app.command()\ndef tui():\n    \"\"\"Launch the Verger Terminal User Interface.\"\"\"\n    try:\n        from verger.tui.tui_app import launch_tui\n\n        launch_tui()\n    except ImportError:\n        typer.secho(\n            \"Error: Textual is not installed. \"\n            \"Please install it with 'pip install textual' or 'uv add textual'.\",\n            fg=typer.colors.RED,\n            bold=True,\n        )\n        raise typer.Exit(code=1) from None\n</code></pre>"},{"location":"reference/utils/#verger.tui.tui_app","title":"<code>verger.tui.tui_app</code>","text":"<p>Verger Terminal User Interface.</p> <p>Main application module for the Verger TUI.</p>"},{"location":"reference/utils/#verger.tui.tui_app.VergerTUI","title":"<code>VergerTUI</code>","text":"<p>               Bases: <code>App</code></p> <p>Verger Terminal User Interface.</p> <p>Main application that manages multiple configuration panes and shared variable inputs for side-by-side evaluation.</p> Source code in <code>src/verger/tui/tui_app.py</code> <pre><code>class VergerTUI(App):\n    \"\"\"\n    Verger Terminal User Interface.\n\n    Main application that manages multiple configuration panes and shared\n    variable inputs for side-by-side evaluation.\n    \"\"\"\n\n    TITLE = \"Verger: The AI Weaver\"\n    CSS_PATH = Path(__file__).parent / \"styles\" / \"main.css\"\n\n    BINDINGS = [\n        (\"q\", \"quit\", \"Quit\"),\n        (\"r\", \"run_engine\", \"Run All\"),\n        (\"a\", \"add_pane_action\", \"Add Pane\"),\n    ]\n\n    def __init__(self, **kwargs: Any):\n        \"\"\"\n        Initialize the Verger TUI.\n\n        Args:\n            **kwargs: Additional parameters for the App.\n        \"\"\"\n        super().__init__(**kwargs)\n        self.engine = ExecutionEngine()\n        self.config = get_config()\n        self.pane_count = 0\n        self._update_timer = None\n\n    def compose(self) -&gt; ComposeResult:\n        \"\"\"\n        Build the UI structure for the main application.\n\n        Yields:\n            The widgets that make up the interface.\n        \"\"\"\n        yield Header()\n\n        with TabbedContent():\n            with TabPane(\"Playground\", id=\"tab-playground\"):\n                # TOP HALF: Horizontal Scroll for Configuration Panes\n                with Vertical(id=\"top-half\"):\n                    with HorizontalScroll(id=\"panes-container\"):\n                        # We will mount panes here dynamically\n                        with Vertical(id=\"add-pane-container\"):\n                            yield Label(\"Add another\\nconfiguration pane\", id=\"add-pane-label\")\n                            yield Button(\"+ Add Pane\", id=\"add-pane-btn\", variant=\"success\")\n\n                # BOTTOM HALF: Shared Variables and Run Button\n                with Vertical(id=\"bottom-half\"):\n                    yield Label(\"[b]Shared Variables[/b]\")\n                    yield VerticalScroll(id=\"variable-inputs\")\n                    with Horizontal(id=\"action-buttons\"):\n                        yield Button(\"Run All Configurations (r)\", variant=\"primary\", id=\"run-btn\")\n                        yield Button(\"+ Add Pane (a)\", id=\"add-pane-btn-bottom\", variant=\"success\")\n                    yield Label(\"\", id=\"status-label\")\n\n            with TabPane(\"Prompts\", id=\"tab-prompts\"):\n                yield PromptsPane(app_ref=self)\n\n            with TabPane(\"Models\", id=\"tab-models\"):\n                yield ModelsPane(app_ref=self)\n\n            with TabPane(\"Tools\", id=\"tab-tools\"):\n                yield ToolsPane(app_ref=self)\n\n            with TabPane(\"Status\", id=\"tab-status\"):\n                yield StatusPane(app_ref=self)\n\n        yield Footer()\n\n    async def on_mount(self) -&gt; None:\n        \"\"\"Handle the application mount event.\"\"\"\n        await self.add_pane()\n\n    async def on_button_pressed(self, event: Button.Pressed) -&gt; None:\n        \"\"\"\n        Handle all button press events.\n\n        Args:\n            event: The button pressed event.\n        \"\"\"\n        if event.button.id in (\"add-pane-btn\", \"add-pane-btn-bottom\"):\n            await self.add_pane()\n        elif event.button.id == \"run-btn\":\n            await self.action_run_engine()\n\n    async def action_add_pane_action(self) -&gt; None:\n        \"\"\"Action for adding a new configuration pane via key binding.\"\"\"\n        await self.add_pane()\n\n    async def add_pane(self) -&gt; None:\n        \"\"\"Dynamically add a new configuration pane.\"\"\"\n        self.pane_count += 1\n        container = self.query_one(\"#panes-container\", HorizontalScroll)\n        add_btn_container = self.query_one(\"#add-pane-container\")\n\n        # Mount the new pane right before the add button\n        await container.mount(ConfigPane(self.pane_count, self), before=add_btn_container)\n\n    def schedule_variable_update(self) -&gt; None:\n        \"\"\"Debounce variable updates to prevent excessive parsing while typing.\"\"\"\n        if self._update_timer is not None:\n            self._update_timer.stop()\n        self._update_timer = self.set_timer(0.5, self._extract_and_update_variables)\n\n    async def _extract_and_update_variables(self) -&gt; None:\n        \"\"\"Extract variables from all message blocks in all active panes.\"\"\"\n        all_variables = set()\n        formatter = string.Formatter()\n\n        # Parse text from all message block text areas\n        for msg_block in self.query(MessageBlock):\n            text = msg_block.query_one(TextArea).text\n            if text:\n                from contextlib import suppress\n\n                with suppress(ValueError):\n                    variables = {\n                        field_name\n                        for _, field_name, _, _ in formatter.parse(text)\n                        if field_name is not None\n                    }\n                    all_variables.update(variables)\n\n        await self._render_variable_inputs(all_variables)\n\n    async def _render_variable_inputs(self, variables: set[str]) -&gt; None:\n        \"\"\"\n        Update the variable input widgets, preserving existing values.\n\n        Args:\n            variables: The set of variable names detected in the prompts.\n        \"\"\"\n        container = self.query_one(\"#variable-inputs\", VerticalScroll)\n\n        # Save existing values\n        existing_values = {\n            input_widget.id.replace(\"var-\", \"\"): input_widget.value\n            for input_widget in container.query(Input)\n            if input_widget.id\n        }\n\n        await container.remove_children()\n\n        if not variables:\n            await container.mount(\n                Label(\"No variables detected in current prompts.\", classes=\"text-muted\")\n            )\n            return\n\n        for var in sorted(variables):\n            value = existing_values.get(var, \"\")\n            await container.mount(\n                Horizontal(\n                    Label(f\"{var}:\", classes=\"input-label\"),\n                    Input(\n                        value=value,\n                        placeholder=f\"Enter value for {var}\",\n                        id=f\"var-{var}\",\n                    ),\n                    classes=\"input-row\",\n                )\n            )\n\n    async def action_run_engine(self) -&gt; None:\n        \"\"\"Run all active configurations concurrently.\"\"\"\n        status_label = self.query_one(\"#status-label\", Label)\n\n        # Collect shared variables\n        variables = {}\n        for input_widget in self.query(\"#variable-inputs Input\"):\n            input_widget = cast(Input, input_widget)\n            if input_widget.id:\n                var_name = input_widget.id.replace(\"var-\", \"\")\n                variables[var_name] = input_widget.value\n\n        status_label.update(\"[yellow]Running configurations...[/yellow]\")\n\n        panes = list(self.query(ConfigPane))\n        if not panes:\n            status_label.update(\"[red]No configurations to run.[/red]\")\n            return\n\n        # Prepare tasks for concurrent execution\n        tasks = [self._run_single_pane(pane, variables) for pane in panes]\n\n        # Run all panes concurrently\n        await asyncio.gather(*tasks)\n\n        status_label.update(\"[green]All runs completed![/green]\")\n\n    async def _run_single_pane(self, pane: ConfigPane, variables: dict[str, str]) -&gt; None:\n        \"\"\"\n        Execute a single pane's configuration with multi-message support.\n\n        Args:\n            pane: The ConfigPane to run.\n            variables: Shared variables for formatting the prompt.\n        \"\"\"\n        model_select = pane.query_one(f\"#model-select-{pane.pane_id}\", Select)\n        output_display = pane.query_one(f\"#output-display-{pane.pane_id}\", TextArea)\n\n        if model_select.value == Select.BLANK:\n            output_display.text = \"Please select a model.\"\n            return\n\n        # Collect all messages from the blocks\n        message_blocks = list(pane.query(MessageBlock))\n        if not message_blocks:\n            output_display.text = \"Conversation is empty.\"\n            return\n\n        prompt_messages = []\n        for block in message_blocks:\n            data = block.get_message_data()\n            # We pass a list of dicts which NativeListResolver can handle\n            prompt_messages.append(data)\n\n        output_display.text = \"Waiting for model...\"\n\n        try:\n            model_name = cast(str, model_select.value)\n            model_ref = self.config.get_model_ref(model_name)\n\n            # Dynamically load the model object\n            model_obj = import_reference(model_ref)\n\n            # Run engine\n            response_message = await self.engine.run(\n                model_obj=model_obj,\n                prompt_obj=prompt_messages,\n                variables=variables,\n            )\n            output_display.text = response_message.content\n        except Exception as e:\n            output_display.text = f\"Error: {e}\"\n</code></pre>"},{"location":"reference/utils/#verger.tui.tui_app.VergerTUI.__init__","title":"<code>__init__(**kwargs)</code>","text":"<p>Initialize the Verger TUI.</p> <p>Parameters:</p> Name Type Description Default <code>**kwargs</code> <code>Any</code> <p>Additional parameters for the App.</p> <code>{}</code> Source code in <code>src/verger/tui/tui_app.py</code> <pre><code>def __init__(self, **kwargs: Any):\n    \"\"\"\n    Initialize the Verger TUI.\n\n    Args:\n        **kwargs: Additional parameters for the App.\n    \"\"\"\n    super().__init__(**kwargs)\n    self.engine = ExecutionEngine()\n    self.config = get_config()\n    self.pane_count = 0\n    self._update_timer = None\n</code></pre>"},{"location":"reference/utils/#verger.tui.tui_app.VergerTUI.action_add_pane_action","title":"<code>action_add_pane_action()</code>  <code>async</code>","text":"<p>Action for adding a new configuration pane via key binding.</p> Source code in <code>src/verger/tui/tui_app.py</code> <pre><code>async def action_add_pane_action(self) -&gt; None:\n    \"\"\"Action for adding a new configuration pane via key binding.\"\"\"\n    await self.add_pane()\n</code></pre>"},{"location":"reference/utils/#verger.tui.tui_app.VergerTUI.action_run_engine","title":"<code>action_run_engine()</code>  <code>async</code>","text":"<p>Run all active configurations concurrently.</p> Source code in <code>src/verger/tui/tui_app.py</code> <pre><code>async def action_run_engine(self) -&gt; None:\n    \"\"\"Run all active configurations concurrently.\"\"\"\n    status_label = self.query_one(\"#status-label\", Label)\n\n    # Collect shared variables\n    variables = {}\n    for input_widget in self.query(\"#variable-inputs Input\"):\n        input_widget = cast(Input, input_widget)\n        if input_widget.id:\n            var_name = input_widget.id.replace(\"var-\", \"\")\n            variables[var_name] = input_widget.value\n\n    status_label.update(\"[yellow]Running configurations...[/yellow]\")\n\n    panes = list(self.query(ConfigPane))\n    if not panes:\n        status_label.update(\"[red]No configurations to run.[/red]\")\n        return\n\n    # Prepare tasks for concurrent execution\n    tasks = [self._run_single_pane(pane, variables) for pane in panes]\n\n    # Run all panes concurrently\n    await asyncio.gather(*tasks)\n\n    status_label.update(\"[green]All runs completed![/green]\")\n</code></pre>"},{"location":"reference/utils/#verger.tui.tui_app.VergerTUI.add_pane","title":"<code>add_pane()</code>  <code>async</code>","text":"<p>Dynamically add a new configuration pane.</p> Source code in <code>src/verger/tui/tui_app.py</code> <pre><code>async def add_pane(self) -&gt; None:\n    \"\"\"Dynamically add a new configuration pane.\"\"\"\n    self.pane_count += 1\n    container = self.query_one(\"#panes-container\", HorizontalScroll)\n    add_btn_container = self.query_one(\"#add-pane-container\")\n\n    # Mount the new pane right before the add button\n    await container.mount(ConfigPane(self.pane_count, self), before=add_btn_container)\n</code></pre>"},{"location":"reference/utils/#verger.tui.tui_app.VergerTUI.compose","title":"<code>compose()</code>","text":"<p>Build the UI structure for the main application.</p> <p>Yields:</p> Type Description <code>ComposeResult</code> <p>The widgets that make up the interface.</p> Source code in <code>src/verger/tui/tui_app.py</code> <pre><code>def compose(self) -&gt; ComposeResult:\n    \"\"\"\n    Build the UI structure for the main application.\n\n    Yields:\n        The widgets that make up the interface.\n    \"\"\"\n    yield Header()\n\n    with TabbedContent():\n        with TabPane(\"Playground\", id=\"tab-playground\"):\n            # TOP HALF: Horizontal Scroll for Configuration Panes\n            with Vertical(id=\"top-half\"):\n                with HorizontalScroll(id=\"panes-container\"):\n                    # We will mount panes here dynamically\n                    with Vertical(id=\"add-pane-container\"):\n                        yield Label(\"Add another\\nconfiguration pane\", id=\"add-pane-label\")\n                        yield Button(\"+ Add Pane\", id=\"add-pane-btn\", variant=\"success\")\n\n            # BOTTOM HALF: Shared Variables and Run Button\n            with Vertical(id=\"bottom-half\"):\n                yield Label(\"[b]Shared Variables[/b]\")\n                yield VerticalScroll(id=\"variable-inputs\")\n                with Horizontal(id=\"action-buttons\"):\n                    yield Button(\"Run All Configurations (r)\", variant=\"primary\", id=\"run-btn\")\n                    yield Button(\"+ Add Pane (a)\", id=\"add-pane-btn-bottom\", variant=\"success\")\n                yield Label(\"\", id=\"status-label\")\n\n        with TabPane(\"Prompts\", id=\"tab-prompts\"):\n            yield PromptsPane(app_ref=self)\n\n        with TabPane(\"Models\", id=\"tab-models\"):\n            yield ModelsPane(app_ref=self)\n\n        with TabPane(\"Tools\", id=\"tab-tools\"):\n            yield ToolsPane(app_ref=self)\n\n        with TabPane(\"Status\", id=\"tab-status\"):\n            yield StatusPane(app_ref=self)\n\n    yield Footer()\n</code></pre>"},{"location":"reference/utils/#verger.tui.tui_app.VergerTUI.on_button_pressed","title":"<code>on_button_pressed(event)</code>  <code>async</code>","text":"<p>Handle all button press events.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>Pressed</code> <p>The button pressed event.</p> required Source code in <code>src/verger/tui/tui_app.py</code> <pre><code>async def on_button_pressed(self, event: Button.Pressed) -&gt; None:\n    \"\"\"\n    Handle all button press events.\n\n    Args:\n        event: The button pressed event.\n    \"\"\"\n    if event.button.id in (\"add-pane-btn\", \"add-pane-btn-bottom\"):\n        await self.add_pane()\n    elif event.button.id == \"run-btn\":\n        await self.action_run_engine()\n</code></pre>"},{"location":"reference/utils/#verger.tui.tui_app.VergerTUI.on_mount","title":"<code>on_mount()</code>  <code>async</code>","text":"<p>Handle the application mount event.</p> Source code in <code>src/verger/tui/tui_app.py</code> <pre><code>async def on_mount(self) -&gt; None:\n    \"\"\"Handle the application mount event.\"\"\"\n    await self.add_pane()\n</code></pre>"},{"location":"reference/utils/#verger.tui.tui_app.VergerTUI.schedule_variable_update","title":"<code>schedule_variable_update()</code>","text":"<p>Debounce variable updates to prevent excessive parsing while typing.</p> Source code in <code>src/verger/tui/tui_app.py</code> <pre><code>def schedule_variable_update(self) -&gt; None:\n    \"\"\"Debounce variable updates to prevent excessive parsing while typing.\"\"\"\n    if self._update_timer is not None:\n        self._update_timer.stop()\n    self._update_timer = self.set_timer(0.5, self._extract_and_update_variables)\n</code></pre>"},{"location":"reference/utils/#verger.tui.tui_app.launch_tui","title":"<code>launch_tui()</code>","text":"<p>Launch the Verger TUI. Core setup is assumed to be handled by the caller.</p> Source code in <code>src/verger/tui/tui_app.py</code> <pre><code>def launch_tui() -&gt; None:\n    \"\"\"Launch the Verger TUI. Core setup is assumed to be handled by the caller.\"\"\"\n    app = VergerTUI()\n    app.run()\n</code></pre>"},{"location":"reference/utils/#verger.main","title":"<code>verger.main</code>","text":"<p>Main entry point for the Verger application.</p>"},{"location":"reference/utils/#verger.main.main","title":"<code>main()</code>","text":"<p>Main execution routine for Verger.</p> <p>This function coordinates the startup sequence: 1. Configures logging. 2. Initializes the core engine (config, plugins). 3. Launches the Typer command-line interface.</p> Source code in <code>src/verger/main.py</code> <pre><code>def main() -&gt; None:\n    \"\"\"\n    Main execution routine for Verger.\n\n    This function coordinates the startup sequence:\n    1. Configures logging.\n    2. Initializes the core engine (config, plugins).\n    3. Launches the Typer command-line interface.\n    \"\"\"\n    try:\n        # Configuration of logs happens at the APP level, not the CORE level.\n        setup_logging()\n\n        # Initialize core (plugins, config, etc.)\n        setup_app()\n\n        # Launch Typer\n        app()\n\n    except Exception as e:\n        print(f\"Error during startup: {e}\", file=sys.stderr)\n        sys.exit(1)\n</code></pre>"},{"location":"reference/utils/#utilities","title":"Utilities","text":""},{"location":"reference/utils/#verger.utils.imports","title":"<code>verger.utils.imports</code>","text":"<p>Utilities for dynamic importing of user objects.</p>"},{"location":"reference/utils/#verger.utils.imports.import_reference","title":"<code>import_reference(ref)</code>","text":"<p>Dynamically imports a module and extracts an attribute based on a reference string.</p> <p>Parameters:</p> Name Type Description Default <code>ref</code> <code>str</code> <p>The reference string, formatted as 'module.path:attribute_name'</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The extracted Python object.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the format is invalid.</p> <code>ImportError</code> <p>If the module cannot be imported.</p> <code>AttributeError</code> <p>If the attribute does not exist in the module.</p> Source code in <code>src/verger/utils/imports.py</code> <pre><code>def import_reference(ref: str) -&gt; Any:\n    \"\"\"\n    Dynamically imports a module and extracts an attribute based on a reference string.\n\n    Args:\n        ref: The reference string, formatted as 'module.path:attribute_name'\n\n    Returns:\n        The extracted Python object.\n\n    Raises:\n        ValueError: If the format is invalid.\n        ImportError: If the module cannot be imported.\n        AttributeError: If the attribute does not exist in the module.\n    \"\"\"\n    if not isinstance(ref, str) or \":\" not in ref:\n        raise ValueError(\n            f\"Invalid reference format: '{ref}'. Expected 'module.path:attribute_name'.\"\n        )\n\n    module_path, attr_name = ref.split(\":\", 1)\n\n    try:\n        module = importlib.import_module(module_path)\n    except ImportError as e:\n        raise ImportError(f\"Could not import module '{module_path}' for reference '{ref}'.\") from e\n\n    try:\n        return getattr(module, attr_name)\n    except AttributeError as e:\n        raise AttributeError(f\"Module '{module_path}' has no attribute '{attr_name}'.\") from e\n</code></pre>"},{"location":"reference/utils/#verger.utils.logging","title":"<code>verger.utils.logging</code>","text":"<p>Logging configuration for the Verger application.</p>"},{"location":"reference/utils/#verger.utils.logging.setup_logging","title":"<code>setup_logging()</code>","text":"<p>Configures the logging for Verger. By default, it shows WARNING and above. Users can set VERGER_LOG=DEBUG to see all logs including plugin discovery.</p> Source code in <code>src/verger/utils/logging.py</code> <pre><code>def setup_logging():\n    \"\"\"\n    Configures the logging for Verger.\n    By default, it shows WARNING and above.\n    Users can set VERGER_LOG=DEBUG to see all logs including plugin discovery.\n    \"\"\"\n    # Get level from environment variable, default to WARNING\n    log_level_str = os.getenv(\"VERGER_LOG\", \"WARNING\").upper()\n\n    # Map string to logging constants\n    level = getattr(logging, log_level_str, logging.WARNING)\n\n    # Create a clean format for CLI usage\n    log_format = \"%(levelname)s: %(message)s\"\n    if level == logging.DEBUG:\n        # Show more details (like the module name) in debug mode\n        log_format = \"%(name)s - %(levelname)s - %(message)s\"\n\n    logging.basicConfig(level=level, format=log_format, stream=sys.stderr)\n</code></pre>"}]}